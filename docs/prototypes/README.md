# 試作物 目次

本番サイトには載せないが、git には残しておきたい試作物の置き場。

**`public/` に置かないこと。** このサイトは `output: 'export'` の静的書き出しなので、`public/` の中身は**そのまま公開 URL になる**。試作ページを置くと誰でも取得できてしまう。

| ファイル | 中身 | 経緯 |
| --- | --- | --- |
| [test-anim.html](test-anim.html) | 文字が色と形を変えながら並び替わるシーケンスのプレビュー。CSS `@property` による色遷移の実験 | 単体で開けるスタンドアロン HTML。CDN 版 Tailwind 2.2.19 を外部から読む。2026-09-11 のファイル構成レビューで `public/` から移動 |
