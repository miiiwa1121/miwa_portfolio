# 開発日誌（2026-09）

## 2026-09-09

### UIデザイン全面リフレッシュ（feature/ui-redesign）

- **背景と目的**:
  - Procedural 3D惑星ワールドの世界観・体験クオリティに2D UI層（ヘッダー、エリアカード、ドック、詳細シート）が追いついておらず、デザイン上の質感・コントラスト・アクセシビリティ・モバイル対応に課題があった。
  - `docs/review.md` に記載されていた複数の中・重大指摘（B-3 モバイルヘッダーの溢れとナビゲーション欠如、B-4 ボタンやバッジのコントラスト比不足、C-1 スキル30個のフラット並び、C-2 詳細ページ脱出手段の不足）を一挙に解消し、洗練されたモダンUIへと刷新する。
  - ユーザーの「ブランチを切ってから実装してみて」というリクエストを受け、新ブランチ `feature/ui-redesign` 上で安全に進行。

- **主な変更と設計判断**:
  - **タイポグラフィとコントラスト（WCAG 2.1 AA 準拠）**:
    - ボタンや主要ラベルのコントラストを大幅改善。薄いグレー `text-gray-400`（コントラスト比2.54で不合格だった箇所）を `text-gray-600` / `text-slate-600`（4.5:1以上）へ引き上げ。
    - プライマリCTAボタンを `orange-500`（白文字で比率2.80）から `orange-600`（ホバー時 `orange-500`、アクティブ時 `orange-700`）へ変更し、視認性と触感を両立。
    - `globals.css` にて `:focus-visible` スタイル（`outline: 2px solid #ea580c`、`outline-offset: 2px`）を統一定義し、キーボード操作のアクセシビリティを確立。
  - **ヘッダー＆モバイル体験（B-3 解消）**:
    - 390px以下の小画面幅でボタン群（言語切替・一時停止・ズーム）が右端から溢れていた問題を、ボタンサイズのレスポンシブ化（`w-10 h-10`〜`w-11 h-11`）とギャップ調整で解消。
    - モバイル用のドロップダウンナビメニューを新設。画面幅が狭くても全5エリア（Products, Skills, Experience, About, Contact）へ直接ジャンプ可能に。
  - **詳細シートの戻る導線（C-2 解消・ヘッダー統合設計）**:
    - 詳細シート展開時、画面外へのスクロール以外の離脱方法が無かった問題を解消。
    - 画面右上に浮遊ボタンを別途置くのではなく、ヘッダー右上の `ZoomControl`（3D惑星非表示中は操作不要）と動的に差し替わる形で「× 閉じる」ボタンを統合。固定ヘッダーの座標やコンテキストと自然に調和するレイアウトとした。
  - **エリアカード（AreaCard）の立体感とタクタイル性**:
    - スタックカードに深いシャドウ（`shadow-2xl shadow-black/35`）とボーダー、質感のあるサブタイトルピルバッジを追加。
    - 「詳しく見る」CTAボタンを鮮やかな高コントラストボタンへ刷新し、ホバー・タップ時のフィードバックを強化。
  - **Skills（C-1 解消）の5カテゴリ構造化**:
    - 30個のスキルチップが平坦に並んでいた一覧を、Frontend / Backend & Languages / Mobile & Native / Cloud & Databases / Tools & AI の5つの明確なカテゴリに分類。
    - 各カテゴリに概要説明とスキル件数バッジを付与。各スキルカードはホバー時に各言語・ツールの象徴カラー（TypeScriptブルー、Goシアン、Dockerブルー、Rustブラウンなど）の光彩をまとうマイクロインタラクションを実装。
  - **Experience & Contact の洗練**:
    - Experienceタイムラインに「現在 / Current」のグリーンパルスバッジと光彩ノードを導入し、最新の活動状況が一目で伝わるように改善。
    - Contactに「ご相談・ご連絡歓迎」ステータスピル、DM on X CTAボタン、ワンタップ「IDをコピー」（コピー完了フィードバック付き）を実装。
  - **Products フィルタとプロジェクトカード**:
    - カプセル型セグメントコントロール（Category & Status）へ刷新。
    - プロジェクトカードはキーボード（Enter/Space）でモーダルを開閉可能にし、アクセシビリティを強化。

- **検証結果**:
  - `npm test`: 21テストファイル・全438テストすべてパス（1.33秒）。既存の3D幾何・カメラ軌道・Aboutクロール等のロジックに破損やデグレがないことを確認。
  - `npm run lint`: エラー・警告ゼロでパス。
  - `npm run build`: Turbopack による静的エクスポート（Next.js 16 `output: 'export'`）が正常完了。
  - ブラウザ実機確認（`localhost:3000`）: デスクトップおよびモバイル幅で全画面（ホーム、各詳細シート、ターミナル、言語切替、プライバシーポリシー）のレイアウト・視認性・操作性を確認。

### Skillsカテゴリの細分化（データベース／クラウド、ツール／AIの分離）

- **背景と目的**:
  - これまで複合カテゴリとなっていた「クラウド & データベース」および「ツール & AI」を、技術ドメインごとの専門性・役割をより明確にするため独立したカテゴリへ分離。
- **主な変更と設計判断**:
  - `src/detail/Skills.tsx`:
    - `cloud-db` を `database`（PostgreSQL, MySQL, SQLite, Supabase）と `cloud`（AWS, GCP, Cloudflare, Vercel）に分離。
    - `tools` を `tools`（Git, GitHub, Cypress, Figma）と `ai`（Claude, Gemini, ChatGPT）に分離。AIカテゴリには既存のClaudeに加え、プロジェクト（Umoja等）や東大松尾研GCI修了実績に紐づくGemini（`SiGooglegemini`）およびChatGPT（`TbBrandOpenai`）を配置。
  - `src/hub/SemanticSEO.tsx`:
    - セマンティックSEOリストの技術カテゴリをSkillsの構造と整合するように更新。
- **検証結果**:
  - `npm test`: 全21ファイル・全438テストすべてパス（1.30s）。
  - `npm run lint`: エラー・警告ゼロでパス。

### PCランチャーボタンのポップアニメーション刷新（Stagger + Spring）

- **背景と目的**:
  - 画面左下のPCアイコン（HubDock）をタップ・ホバーした際に出現する3つのボタン（GitHub, X, Terminal）が単純なフェードインで表示されていたため、サイトの世界観（遊び心・タクタイル感）に調和する弾むようなスプリング展開アニメーションへと刷新。
- **主な変更と設計判断**:
  - `src/hub/HubDock.tsx`:
    - `framer-motion` の `motion` および `AnimatePresence` を適用。
    - PCアイコン自体のタップ・ホバー操作フィードバック（`whileHover`, `whileTap`）およびオープン時の微傾斜（`rotate: 8`）を追加。
    - 3つのアクションボタンがPCアイコンの裏側から1つずつポンポンと飛び出すスタッガー（時間差 0.07s）× スプリング物理（`stiffness: 480, damping: 22, mass: 0.8`）アニメーションを実装。
    - 閉じる際も `AnimatePresence` により逆順（Terminal → X → GitHub）で素早く吸い込まれるようなイグジットアニメーション（`staggerDirection: -1`）を構成。
- **検証結果**:
  - `npm test`: 全21ファイル・全438テストすべてパス（1.02s）。
  - `npm run lint`: エラー・警告ゼロでパス。
  - ブラウザ実機確認: タップおよびホバー時の小気味よいポップアウトと収納動作を確認。

### エリアカードの3Dデッキシャッフル切り替えアニメーション実装

- **背景と目的**:
  - ホーム画面左側のエリア説明カード（AreaCard）の切り替えが単調な上下スライドだったため、トランプの山札のように立体的で触り心地の良い「3Dデッキ・カードシャッフル」アニメーションへと刷新。
- **主な変更と設計判断**:
  - `src/hub/AreaCard.tsx`:
    - カードラッパーに 3D パースペクティブ（`[perspective:1000px]`）と `transform-style: preserve-3d` を適用。
    - 切り替えアニメーション `CARD_VARIANTS` を刷新:
      - 退出カード: スワイプ・スクロール方向に合わせて山札からめくられるように上/下へスライドしつつ、立体的な傾き（`rotateZ: ±6.5deg`, `rotateX: ±10deg`）と微細なスケールダウン（`0.91`）、ブラーを伴ってスムーズに抜ける演出（0.28s）。
      - 進入カード: 山札の奥から逆方向の微細な傾き（`rotateZ: ∓4.5deg`, `rotateX: ∓8deg`）とスケール（`0.93`）から手前へと立ち上がり、スプリング物理演算（`stiffness: 340, damping: 26, mass: 0.85`）でピタッと正面に収束。
    - 背後の山札ピークレイヤー（PEEK_OFFSETS）に僅かな有機的角度（`-1.2deg`, `+1.5deg`）を付与し、物理的なトランプデッキのような重なり感を表現。
    - ホバー時にカードを少し持ち上げるようなマイクロチルト演出（`whileHover: { y: -5, rotateZ: 0.8, scale: 1.01 }`）を追加。
- **検証結果**:
  - `npm test`: 全21ファイル・全438テストすべてパス（1.13s）。
  - `npm run lint`: エラー・警告ゼロでパス。
  - ブラウザ実機確認: ホイール操作およびスワイプ操作によるカード切り替えで、トランプデッキのような滑らかで心地よい立体シャッフルアニメーションを確認。




## 2026-09-11

### ファイル構成の通しレビューと、その場での是正

- **背景と目的**:
  - 機能追加を重ねた結果として、命名・配置・粒度がどこまで一貫しているかを一度通しで見た。**プロダクトの挙動は一切変えず、置き場と名前だけを直す**という方針で、レビューで挙がった全項目をそのまま是正した。
  - 出発点は 438テスト・lint・型・ビルドすべてクリーンな状態。**終了時も同じであることを各段階で確認しながら進めた**（挙動を変えていないので、テスト件数が増減したらそれ自体が事故のサイン）。

- **消したもの・`public/` から出したもの**:
  - `src/scene/VoxelIsland.tsx`（91行）を削除。惑星移行で `planet/shell.ts` に置き換わっており [planet-migration.md](../planet-migration.md) にも「済」と書いてあるのに、**ファイルだけが残っていた**。どこからも import されていない完全な死にコード。
  - `public/favicon.ico` / `public/icon.png` / `public/icon.svg` を削除。`src/app/` 側と**md5 まで同一**で、かつ App Router のファイル規約で効いているのは `src/app/` 側だけ。`public/` 側はコードからもメタデータからも参照ゼロだった。ビルド後の `out/` に `favicon.ico`・`icon.png`・`icon.svg`・`apple-icon.png` が従来どおり出ることを確認済み。
  - `public/test-anim.html`（60KB）を `docs/prototypes/` へ移動。`output: 'export'` なので `public/` の中身はそのまま配信される＝**試作ページが本番の公開URLになっていた**（中身は CDN 版 Tailwind 2.2.19 を外部から読む文字アニメの実験）。
  - `public/models/`（1.5MB）を `assets/models/` へ移動。これは [review.md](../review.md) C-6 が挙げていた項目で、**解決策として書かれていた移動先をそのまま実行**し、台帳から C-6 を削除した。
  - 上記2つで、**配信されるが誰も読まないファイルが 1.56MB 減った**（`out/test-anim.html` と `out/models/` が消えたことを確認）。

- **フォルダに分けたもの**:
  - `src/scene/` 直下が16ファイル（R3Fコンポーネント9＋純粋モジュール7）とフラットすぎたので、`camera/`（構図・飛行・射影）と `objects/`（シーンに置かれるもの）に割った。`planet/` と `voxel/` は既に切れていたので、**直下に残すのは層をまたぐもの**（`sceneClock` / `markerBolt` / `sunLight` / `planetPlacement`）と `Scene.tsx` だけになった。
  - `src/hub/` のカード関連5モジュール＋テスト2本を `hub/card/` へ。consumer を調べると `Hub → SectionCard → {cardWheel, useCardGestures}` と `Hub → CardLeaderLine → lightningPath` で**完全に閉じている**ことが分かったので、移動で外に漏れる import が無い（実際、動かしたのは5ファイル、import を直したのは `Hub.tsx` と `CardLeaderLine.tsx` だけ）。`hub/about/` という同じ粒度の前例があるので形も揃う。

- **改名したもの（なぜその名前だったのかの記録も含む）**:
  - `scene/worldLayout.ts` → `scene/camera/cameraLayout.ts`、`scene/planet/planetLayout.ts` → `scene/planet/geometry.ts`。**どちらも「Layout」だったせいで、名前から役割が引けなかった。** 前者は惑星の話をほとんどせずカメラの構図を持つのに `world`、後者が `planet` という逆転もあった。
  - `scene/Diorama.tsx` → `scene/objects/PlanetScene.tsx`。`Diorama` は平面の浮遊島時代の名前で、中に `Planet.tsx` を含む階層が名前から読めなかった。**散文としての「ジオラマ」は残す**——コード内に35箇所、README にも「ボクセル・ジオラマ」とあり、3Dシーン全体を指す語として定着している。ファイル名でなくなったので衝突しない。
  - `scene/AreaMarkers.tsx` → `SectionMarkers.tsx`、`hub/AreaCard.tsx` → `hub/card/SectionCard.tsx`。同じ5エリアを指す語を数えたら **`section` 系が218箇所に対し `Area` はこの2コンポーネント名だけ**だった。少数派を多数派に寄せた。
  - `detail/Section.tsx` → `detail/DetailSection.tsx`。`SectionType`（サイトの5エリア）と `Section`（詳細ページの見出しコンテナ）が**同じ語で別物を指していた**。
  - `data/project.ts` → `data/projectModel.ts`。`projects.ts`（データ本体）と1文字違いで取り違えやすかった。バレル `data/index.ts` があるので外から見た import 文は変わっていない。
  - `scene/objects/VoxelBus.tsx` を named export → default export。1ファイル1コンポーネントの `.tsx` は33個あり、**named なのはここだけ**だった（`Decorations.tsx` と `ProceduralObjects.tsx` は5コンポーネントずつ束ねているので named が妥当）。

- **重複していた真実を1つにしたもの**:
  - マーカーの寸法比4つ（`MARKER_CLEARANCE` / `MARKER_GLYPH_FILL` / `MARKER_GLYPH_RIM` / `MARKER_GLOW_FILL`）を `cameraLayout.ts` から `markerBolt.ts` へ移した。**CLAUDE.md は「形・色・火花は markerBolt.ts に集約」と書いていたのに、実装では寸法だけカメラ側に残っていた。** 痕跡は `SectionMarkers.tsx` が「マーカーの見た目」を作るのに**2つの import ブロックを持っていた**こと。移動後は `markerBolt` 側に1本化され、`cameraLayout` は射影（`pixelsPerWorldUnit` / `markerScaleForScreenRadius`）だけを残す。`markerBolt.ts` は import を1つも持たない純粋モジュールなので、`cameraLayout → markerBolt` の一方向依存になり循環しない。
  - `AppStateContext` の `export type { SectionType }` / `export type { OrbitZoom }` を撤去。**同じ型に2つの import 経路があり**、15ファイルは `@/types` から、`Scene.tsx` と `Hub.tsx` だけ Context 経由で読んでいた。定義元から直接読む形に統一。

- **是正しなかったもの（理由の記録）**:
  - `public/images/` の `gashaan.png` / `umoja.png` / `reallog.png` / `thanks_log.png`（計360KB）は、grep 上は未参照だが**削除しない**。`projects.ts` を見ると、この4つは `status: "dev"` の4案件そのもので、データ側がまだ `coming_soon.png` を指しているだけだった。つまり死蔵ではなく**先に入った素材**で、[review.md](../review.md) B-7「制作実績の半分が空」の待ち状態。**「参照ゼロ＝不要」と短絡すると、まだ出番が来ていない素材を消す。**
  - `src/ui/` はファイル1つ（`icons.tsx`、12行）だがフォルダのまま残す。`hub/HubDock.tsx` と `detail/products/ProjectLinks.tsx` という**異なる2層から使われている**ので、共有UIの置き場として正しい。`src/` 直下に出すとコンポーネントが `types.ts` の隣に並ぶぶん、かえって収まりが悪い。

- **踏んだ罠**:
  - **BSD sed は BRE の `\|` 選択を解さない。** `s#(\.\./src/scene/\(Planet\|FillerCity\)\.tsx)#...#` が**エラーも出さず単に一致しない**ので、置換できたつもりで docs のリンクが切れたままになっていた。`-E`（ERE）に切り替えて解決。同様に `\b` も効かないので、`s/\bAreaCard\b/SectionCard/g` が無言で何もしなかった。**sed が0件置換でも成功終了する**以上、置換後に grep で確認しないと気づけない。
  - **テストだけでは移動の失敗を検出できない。** 438件は全部パスしているのに `tsc --noEmit` が `hub/card/CardLeaderLine.tsx` の `@/scene/markerScreen` 解決失敗を1件出した。**テスト対象は純粋ロジックだけ＝`.tsx` を1つも通らない**という方針の裏返しなので、構成を触る作業では `npm test` と `tsc --noEmit` と `npm run build` の3つを揃えて回す。

- **docs の追随**:
  - `CLAUDE.md` に「フォルダの分け方」節を追加し、`scene/` の5つの置き場と「直下に足さない」を明記。あわせて `SectionType` の単一経路、export の規約、`public/` に置いてよいものを不変条件として書いた。
  - `README.md` のディレクトリ構成図を実態に更新。**`scene/planet/` が図から漏れていた**（惑星移行で6モジュール増えたのに `scene/voxel/` だけが載っていた）ので、今回の `camera/` `objects/` `hub/card/` とあわせて追加。
  - `docs/tech.md` はパス参照を更新し、マーカーの節に「寸法比も markerBolt に集約、カメラ側は射影だけ」を追記。
  - `docs/devlog/*` と `docs/planet-migration.md` の**本文は書き換えない**（その時点の記録なので旧名のままが正しい）。ただし `planet-migration.md` の markdown リンクだけは現在地へ向け直し、削除した `VoxelIsland.tsx` へのリンクは「削除済み」の表記に置き換えた。docs 全体の相対リンク・パス参照をスクリプトで総当たりし、**切れゼロ**を確認。

- **検証結果**:
  - `npm test`: 全21ファイル・全438テストパス（レビュー前と同一。挙動を変えていないので件数が動かないことが正しさの条件）。
  - `npx tsc --noEmit`: エラーゼロ。
  - `npm run lint`: エラー・警告ゼロ。
  - `npm run build`: 静的書き出し成功（10ページ）。`out/` に `test-anim.html` と `models/` が無く、`favicon.ico` / `icon.png` / `icon.svg` / `apple-icon.png` は従来どおり出ることを確認。

### CLAUDE.md の分割 — 指示書と、判断の理由を分ける

- **背景と目的**:
  - `CLAUDE.md` が 45,526 バイトまで肥大していた。**セッション開始時に毎回読み込まれるファイル**なので、この大きさはすべての作業に固定費として乗る。
  - 中身を測ると「3D シーンを触るときの前提」だけで **29,856 バイト＝全体の 65%** を占めていた。読むと、そのほとんどが**ルールではなく判断の理由**（実測値・却下した案・詰まった経緯）で、本来 docs にあるべきものだった。
  - 目的は「短くする」ことではなく**役割を分ける**こと。`CLAUDE.md` は「何を守るか」、`docs/` は「なぜそうなのか」。

- **やったこと**:
  - `docs/scene-invariants.md`（新規、30,711バイト）— 3D の不変条件51項目を、理由・実測値・却下案ごと移設。9つの節（球面という前提／カメラ 姿勢と構図／カメラ 自由回転の高度と復帰／カメラ 行き先と飛行／自己紹介／光と影／マーカー・点線・カード／React・R3F の境界／描画とオブジェクトの配置）に再編した。**フラットな51個の箇条書きは探せない**ので、分類し直すこと自体が移設の目的の半分。
  - `docs/testing.md`（新規）— テスト方針と変異テストの教訓。
  - `docs/verification.md`（新規）— ヘッドレスでの見た目確認の手順と、計測の落とし穴。
  - `docs/structure.md`（新規）— ディレクトリ構成と命名の規約（同日のファイル構成レビューで CLAUDE.md に追記したばかりのものを、そのまま移設）。
  - `docs/README.md` / `docs/devlog/README.md` / `docs/prototypes/README.md`（新規）— **`docs/` 配下に README が1つも無かった**。ディレクトリごとに目次を置く規約に合わせて作成。
  - `CLAUDE.md` を書き直し。**45,526 → 8,925 バイト（19.6%）。** 残したのは、ミッション・言語・参照範囲・Next.js 16 の注意・技術スタックの一行要約・コマンド・**「作業前に読むもの」の対応表**・分野をまたぐ原則7つ・docs のルール・ボクセル制作の要点・`.claude/` の位置。

- **設計判断**:
  - **不変条件を CLAUDE.md から消すのは、保護を弱めることではないか**——これが唯一の懸念だった。答えは「**読むタイミングを指示に変える**」。`CLAUDE.md` に「変更の種類 → 先に読む docs」の表を置き、3D に触る前は `scene-invariants.md` を**必読**と明記した。常時コンテキストに載せる代わりに、必要なときに確実に読ませる形にしている。
  - **移設は手で書き写さずスクリプトで行った。** 51項目・約3万バイトを転記すると、要約や言い換えが混ざって**実測値や却下理由が静かに落ちる**。行番号で切り出して再配置し、移設後に「元の51行がすべて新ファイルに含まれるか」を機械的に照合してゼロ欠落を確認した。testing / verification / structure も同様。
  - **devlog の命名は `YYYYMM.md`（月次）のまま据え置いた。** 提示された標準テンプレートは `yyyymmdd.md`（日次）だが、このプロジェクトは既に3ファイル・約39万バイトの蓄積があり、`## YYYY-MM-DD` の日付見出しで日単位の区切りは足りている。日次へ割ると履歴が断片化し、docs 各所の「devlog 202607」という参照も全部切れる。**規約を揃える利得より、既存の蓄積を壊す損失が大きい**と判断した。
  - **`docs/devlog/README.md` に「旧名 → 現在」の対応表を置いた。** devlog は本文を書き換えない方針なので、読み手が `worldLayout.ts` や `AreaCard.tsx` を探して見つからない事態が起きる。目次側で吸収する。

- **踏んだ罠**:
  - **移設でリンクの基準が変わることを忘れていた。** `CLAUDE.md`（リポジトリルート）にあった `[docs/tech.md](docs/tech.md)` は、`docs/` 配下へ移すと `docs/docs/tech.md` を指す。移設直後に一括で `](docs/` → `](` へ置換して解消。
  - **リンク検査を「リポジトリルートからの解決」で書いていたため、既存のバグを見逃していた。** `docs/tech.md` の中の `[docs/review.md](docs/review.md)` は、ルート基準では実在するので通ってしまう。**各ファイルのあるディレクトリを基準に解決する**検査に書き換えたところ、この1件と、devlog から改名済みファイルへのリンク1件が出た（どちらも修正済み）。全 md の相対リンクを総当たりして切れゼロを確認。
  - 「上記のとおり」のような**文書内の位置に依存した参照**が、移設で宙に浮く。`structure.md` の1箇所を実際のリンクへ差し替えた。

- **検証結果**:
  - 全 `.md` の相対リンクを総当たり: 切れゼロ。
  - 移設の欠落検査: 元の51行＋testing/verification/structure の全行が新ファイルに存在することを機械照合。
  - `npm test` 438件パス・`npx tsc --noEmit` エラーゼロ・`npm run lint` クリーン（コードは触っていないが、docs 更新前後で環境が壊れていないことの確認）。

### スマホ実機相当の監査 — 何が壊れているかを実測した

- **背景と目的**:
  - 「スマホ画面を洗練したい」という依頼に対し、**推測で直す前に、390×844（iPhone 14 相当・`isMobile` / `hasTouch` 有効・DPR 2）で実際に何が起きているかを測った。** このサイトはブレークポイントによる Tailwind クラスの出し分けしか持っておらず、`matchMedia` / ポインタ種別 / タッチ判定はコード全体に **1箇所も無い**（grep 一致ゼロ）。つまり「スマホ向けの分岐」という概念がまだ実装に存在しない状態だった。
  - 手順は [verification.md](../verification.md) のとおり。スクラッチパッドの `puppeteer-core` ＋ SwiftShader、`networkidle2` の後 10〜11 秒待ってから撮影。カメラの飛行を挟む操作は 9 秒ずつ置いた。

- **測って分かったこと（症状と原因）**:
  - **惑星が惑星として読めるのは `far`（俯瞰）だけ。** `near`（標準）と `building` では画面下 55〜60% が地面で埋まり、丸みが一度もフレームに入らない。原因は構図が横長前提であること自体ではなく、**縦長では `frameDistance` が幅側の半画角で距離を決める**（`Math.min(halfVertical, halfHorizontal)`）ため、アスペクトが 0.46 まで落ちると被写体が縦方向に大きくはみ出すこと。
  - **カードが被写体を完全に覆う。** `CARD_SHARE = 0.3` は「フレーム幅の 3 割をカードに譲る」設計だが、390px 幅でカードの実測は **x = 28〜362（幅 334px、画面の 86%）**。3 割 ≒ 117px 横にずらしても被写体はカードの裏から出ない。`building` のスクショで、寄ったはずの建物が一切見えていないのがその帰結。`ORBIT_CARD_SHARE = 0.22` も同様。**この 2 定数は「カードは画面の一部しか占めない」を暗黙の前提にしている。**
  - **画面下で 3 つの UI が重なる。** `building` のとき、左下のリンクボタン（`HubDock`）・中央の HOME ボタン（`Hub`）・フッター（`HubDock` 内 `absolute bottom-9 right-12`）が同じ帯に入る。
  - **フッターが右端から見切れる。** `right-12`（48px）固定＋折り返し無しのため、390px では「Privacy Polic」で切れる。**`far` / `building` / About の 3 状態すべてで再現。**
  - **詳細シートの上にリンクボタンが残る。** `HubDock` は `pageOpen` をフッターの出し分けにしか使っておらず、🖥 ボタンは白いシートの上に浮いたまま。
  - **モバイルメニューがシートの上に居座る。** Products を開いた直後のスクショでドロップダウンが開いたままタイトルを覆っていた。
  - **About のクロールがタッチでほぼ動かない。** 実測で **500px 指を滑らせて進んだのは 73px**（うち約 32px は 0.5 行/秒の自動再生ぶん、正味 ≒ 40px）。原因は `scrollCatchUp` の rAF ループが毎フレーム `scrollTop` を `scrollTargetRef` へ引き戻すこと。**ホイールには専用ハンドラがあるがタッチには無い**ので、ネイティブのタッチスクロールが書いた `scrollTop` が即座に打ち消される。
  - **`h-screen` と `window.innerHeight` の不一致。** `Sheet` のスペーサーは `h-screen`（= 100vh = large viewport）、`Hub` の `handleScroll` / パーク位置の計算は `window.innerHeight`（= ツールバー表示中は small viewport）。ヘッドレスでは両者 844 で一致するので**この環境では再現しない**が、iOS Safari では約 100px ずれ、シートを開いた直後に上端が浮く。
  - **セーフエリア未対応。** `p-7`（28px）のみで `env(safe-area-inset-*)` の参照がゼロ。
  - **タップ領域。** Privacy Policy 105×16px、プロダクト内リンク 203×21px、ロゴ 81×32px。ヘッダーの丸ボタン群は 44×44 で足りている。
  - **横スクロールは無し**（`documentElement.scrollWidth` = `innerWidth` = 390）。**ランタイムエラーもゼロ**（`pageerror` / `console.error` とも全状態で 0 件）。

- **測るときに踏んだ罠**:
  - **About の `scrollTop` は外から書いても戻される。** `page.evaluate` で `scrollTop = 600` と書いた直後に読むと 172 になっていた。rAF の catch-up ループが `scrollTargetRef` へ引き戻すため。**About を任意の位置で撮るには `scrollTargetRef` を動かす入力（ホイール / タッチ）を送るしかない。** これは不具合ではなく設計（`scrollCatchUp` の doc コメント参照）だが、スクショの撮り方としては verification.md の「別実行で測った値を使い回さない」と同じ種類の落とし穴。
  - **クロールの「読める帯」は縦長で極端に狭い。** 390px 幅では `t = 0.2` で段落の高さが既に 27px、`t = 0.4` で 8px。1440×900 では同じ `t = 0.2` で 52px ある。`rotateX(110deg)` + `perspective: 460px` が、ビューポートが細いほど速く消失点へ持っていく。**適当な位置でスクショを撮ると「文字が出ない」と誤読する**（実際に 3 枚撮り直した）。

- **この時点では実装していない。** 監査だけを記録し、変更は別の指示に従って `feature/mobile-refinement` で行う。

## 2026-09-12

### スマホ UI の作り直し — ボタンを指に置き換え、カードを画面下のレールにする

- **背景**: 前日の監査（[2026-09-11](#スマホ実機相当の監査--何が壊れているかを実測した)）で出た欠陥に対し、依頼者から7点の変更指示が出た。**このコミット群はスマホ（`lg` 未満のタッチ画面）だけを変える。PC の見た目と操作は一切変えていない**（1440×900 で確認済み: ナビ・言語・停止・ズーム・左のカード・左下のドック・右下のフッターすべて従来どおり）。

- **「スマホか」を1箇所で決める（`state/useHandheld.ts`）**:
  - 変更の大半は CSS では書けない**挙動**（ボタンをジェスチャに置き換える、カードの形を変える、カメラの構図を変える）なので、判定を JavaScript に持つ必要があった。このリポジトリには `matchMedia` もポインタ種別の判定も**それまで1つも無かった**。
  - 述語は `(pointer: coarse) and (max-width: 1023px)`。**両方が要る**——`pointer: coarse` だけだとタッチ対応ノートPCからボタンを奪う（マウスもあり、ピンチする相手がいない）。幅だけだと狭くしたデスクトップのウィンドウから奪う（代わりのジェスチャがそもそも存在しない）。幅は Tailwind の `lg`(1024px) に合わせ、CSS 側の切り替わりと JS 側の切り替わりが同じ瞬間に起きるようにした。
  - `useSyncExternalStore` で、サーバー側スナップショットは `false`。`output: 'export'` なので**プリレンダー時に window が無い**——サーバーと最初のクライアントレンダーで答えが違うとハイドレーション不一致になる。PC の答えを返して直後に訂正するのが、唯一ちぎれない選択肢。
  - **Scene 側は Hub から渡されるのではなく自分で同じフックを呼ぶ。** 片方だけ古い答えでレンダーされる状態を作らないため。

- **1. ズームボタンの廃止（スマホのみ）**: ピンチは**2段のまま**（`near`/`far`）。最大ズーム＝「正面の建物」はマーカーのタップで到達する。ピンチを3段に拡張する案は、依頼者の判断で不採用。`ZoomControl` は PC に残っている。

- **2. 停止ボタン → 空タップ（`scene/Scene.tsx` の `useTapGesture`）**:
  - 「何もないところ」の除外は3種類あり、**それぞれ別の仕組みでしか判定できない**。(a) 本物のコントロール（ヘッダー・レール・ターミナル）は DOM を遡って判定（既存の `isInteractive`）、(b) シーン内のオブジェクト（マーカー・太陽）は DOM から見えない——どれも同じ canvas に当たる——ので**自分で名乗る**（新設の `scene/pointerClaim.ts`）、(c) 実はドラッグだった／2本目の指が来た、は自分で候補を捨てる。
  - `pointerClaim` は R3F の `onPointerDown` で `claim(pointerId)` し、`window` 側が `isClaimed` で見る。**この順序が成立するのは DOM のバブリング順のおかげ**で、R3F は canvas のコンテナ、`useViewInput` は `window` で聞いているため、オブジェクトの `onPointerDown` は必ず先に走る。`Sun` が既に同じ順序に依存している（太陽のドラッグが惑星も回さないように）。
  - タップの条件は**移動 10px 未満かつ 350ms 未満**。`useViewInput` は `pointerdown` で即 `draggingRef = true` にしていて**移動量の閾値を持っていなかった**ので、閾値はこちらに新設した。
  - `useViewInput` に分岐を足すのではなく**別フック**にした。あちらは「カメラがどこを向くか」、こちらは「もう無いボタン」の話で、共有しているのはイベントの流れだけ。
  - YouTube 風のバッジ（`hub/PauseFlash.tsx`）は 0.6 秒で消える。**タップのときだけ**——PC は停止ボタンの塗りが状態を語っているので、そこで光らせるのは画面にあるものを言い直すだけ。
  - **踏んだ罠**: バッジを `opacity: 0` までアニメーションして**アンマウントしていなかった**。見えないが全画面 fixed の層が残り、以後のヒットテストを全部通る。`onAnimationComplete` で親に伝えて消す形に直した（nonce を照合してから消す。フェード中の2度目のタップが既に置き換えている場合があるため）。

- **4. ハンバーガー → 全画面パネル（`hub/MobileMenu.tsx`）**: 旧ドロップダウンは `z-50` で**詳細シート（z-20）のタイトルを覆っていた**うえ、390px 幅で 190px のドロップダウンに5項目が詰まっていた。全画面にしたことで、行き場を失ったもの（外部リンク・著作権表記）の置き場も同時に確保できた。閉じるボタンはハンバーガーと**同じ座標**に置き、「ボタンが開いた」ように見せている。

- **5. ヘッダーは ロゴ / 言語 / メニュー**: 停止とズームが 1・2 で消えたので、スマホの右側は2つだけになった。**シートが開いているときは3つ目の枠が「閉じる」に入れ替わる**（依頼者の指定）。読んでいる最中に別のセクションへ行くことは、まずその読書を閉じることでもあるので、2つ並べる必要がない。

- **6. カードを画面下のレールへ（`hub/card/CardRail.tsx` / `railLayout.ts`）**:
  - **5枚を横に並べるのではなく、3枚の「窓」がカメラと一緒に動く。** ツアーは閉じた輪なので、固定の5枚の帯にすると `TOUR_ORDER` の端をまたぐときに端から端へ飛ぶ——その継ぎ目は実在の場所ではなく、輪を直線に並べた副作用でしかない。窓なら継ぎ目が無い。390px で隣2枚が覗ける幅としても3枚が上限。
  - **一方向のまま。** ドラッグは `onStep` に「要求」を出し、カメラが回り、正面になったエリアが `facing` として降りてきてレールを動かす。ネイティブのスクロールコンテナにしなかったのはこのため——スクロール位置は**それ自体が状態**で、カメラがそれを追いかけると相互更新の輪が閉じる（[scene-invariants.md](../scene-invariants.md)「カードとカメラを相互に更新しない」）。
  - **左へ引くと前進**（`stepForRailDrag`）。帯の並びは `TOUR_ORDER` の添字の向きで、左へ引けば右のカードが中央に来る＝アイドル回転が自分で進む先と同じ。逆だと惑星が親指に逆らって回る。
  - **縦優先のドラッグは拒否する。** レールの上を縦に払うのは惑星を傾けようとしている手で、そこにステップを返すとレールが惑星向けの操作を横取りしているように感じる。
  - 「詳しく見る」ボタンはカードから外し、**カード右上の外側の丸ボタン**にした（依頼者の指定）。ここは点線の引き出し線の起点でもある。**アンカーはカードの中ではなくレール側の固定マークアップに置く**——カードは `AnimatePresence` で作り直されるので、中に置くと退場側の ref コールバックが後から `null` で呼ばれて線が凍る。
  - 待ち合わせのゲート（到着するまで次のステップを受け付けない）は PC のカードと共通なので `useStepGate.ts` に切り出した。カードの文言も `cardCopy.ts` に切り出して2箇所に持たないようにした。

- **7. 著作権表記と Privacy Policy をメニューへ**: メイン画面から撤去。390px では `right-12` 固定＋折り返し無しで**「Privacy Polic」と見切れていた**（`far`／`building`／About の3状態で再現）ので、不具合の解消も兼ねている。

- **HOME ボタンはスマホから削除**: 実測で、レール・リンクボタン・フッターと**同じ帯に4つが重なっていた**。抜けるのはピンチアウト、完全リセットはロゴ。

- **検証**（すべて 390×844 / DPR2 / `isMobile`・`hasTouch` 有効のヘッドレスで実測）:
  - 空タップ → 停止: タップ前後で画面の差分画素が **74,430 → 16**（完全停止）、⏸ バッジ表示。もう一度で **80,632**（再開）、▶ バッジ。
  - 惑星のドラッグ → バッジ「なし」（誤爆なし）。
  - レールの横スワイプ → カードが1つ進む（技術スタック → 経歴・活動）。バッジ「なし」。
  - カードのタップ → `#skills` が開く。
  - **マーカーのタップ → `#skills` へ遷移し、バッジ「なし」**（`pointerClaim` が効いている）。
  - ピンチ: 惑星の占有率 89% →(すぼめる) 51% →(ひろげる) 82%。バッジ「なし」。
  - ランタイムエラー 0 件、横スクロール無し。`npm test` 462件、`tsc --noEmit`・`lint`・`build` すべてクリーン。

### スマホのカメラ構図 — `NEAR_VERTICAL_SHARE` は触らず、別定数を実測で決めた

- **最初の導出が間違っていた（記録として残す）**: 「カードが下に来るのだから、惑星を上へ持ち上げて空けよう」と考え、`HANDHELD_VERTICAL_SHARE = HANDHELD_HEADER_SHARE - HANDHELD_RAIL_SHARE = -0.147` という**負の値**を置いた。導出自体は筋が通っていたが、**前提が違った**——「惑星の円盤をヘッダーとレールの間に収める」という式は `far` の話で、`near` では惑星は円盤ではなく**画面いっぱいの風景**である。実測すると地面が**フレームの94%**を覆い、水平線は画面の上に消えていた。
- **`near` でのリーンは「円盤を置く」ためではなく「水平線を入れる」ためにある。** 390×844 で実測したところ、空と地面の境界は share に対して完全に線形だった:

  | share | 水平線の y | 地面の占有率 |
  | --- | --- | --- |
  | 0.0 | 25 | 91% |
  | 0.20 | 121 | 88% |
  | 0.35 | 186 | 80% |
  | 0.50 | 256 | 71% |
  | 0.636（PC） | 324 | 65% |

  近似すると `水平線y ≈ 25 + 470·share`。

- **上限を決めるのは構図ではなくマーカー**: リーンはカードが説明しているエリアを画面下へ押し、スマホではそこにレールがある。**ツアーの u=0（ロゴのリセット）から一周ぶん20点**を、レール上端の実測値 648px に対して測った:

  | share | レールに隠れる | 最悪の y |
  | --- | --- | --- |
  | 0.15 | **2/20** | 695 |
  | 0.20 | 8/20 | 704 |
  | 0.25 | 14/20 | 796 |
  | 0.30 | 19/20 | 820 |

  **0.15 が折れ点。** 隠れるのは2点だけで、どちらも50px 未満。これ以上は「空を厚くする代わりに一周の大半で点線を消す」買い物になる。
- **踏んだ罠（計測側）**: 最初の測定は**アイドル回転が進んだ任意の位置から**サンプリングしていたため、0.20 が 0.15 より良くも悪くも出た。[verification.md](../verification.md) の「一周ぶんサンプリングして最悪角度で判断する」は、**開始点を揃えることまで含む**。ロゴのクリック（ツアーの u=0 へのリセット）を共通の開始点にして解決した。
- **PC 側は 1mm も動かしていない。** `NEAR_VERTICAL_SHARE` は `reference/image7.png`——PC のスクショの上に描いたモックアップ——から導かれた値で、下端にレールのある 0.46 アスペクトのフレームについては**何も言っていない**。スマホは「その値が間違っていた場面」ではなく「その値が扱っていなかった場面」。
- **副産物: スマホでは点線の引き出し線が生きる。** PC では構図を優先して捨てられている機能（0.636 で20点中16点が画面外）が、0.15 では20点中18点で描ける。レールの矢印ボタンが「このカードはあの場所の話だ」と言えるようになった。
- **横寄せ（`CARD_SHARE` / `ORBIT_CARD_SHARE`）はスマホで 0 にした。** 390px でカードは幅の**86%**（実測334px）を占めるので、幅の22%や30%を譲っても被写体はカードの裏から出ない——別の部分の裏に移るだけ。仕組みは `focalOffsetX()` 一本のままで、share の数だけを変えている（「セクションのときだけ明示的に0に戻す」という二本立ての規則は増やさない、という不変条件を守るため）。

### スマホ UI の手直し — ボタンが押せない原因と、出口の矢印

- **「ハンバーガーとバツが押せない」の正体は Next.js の開発インジケーターだった。** アプリ側のバグではない。390px 幅で `#devtools-indicator` は `[334,20,36,36]` に描かれ、ヘッダー右端のボタン `[318,28,44,44]` に重なる。`document.elementFromPoint` をボタンの中心で撃つと、返ってくるのは `BUTTON` ではなく **`NEXTJS-PORTAL`**——つまり本当に押せない。言語ボタンとロゴは無事だった（この2つは重なっていない）ので、症状が「右端の1つだけ効かない」形で出ていた。
  - **`devIndicators: { position: 'bottom-left' }` では動かなかった。** `node_modules/next/dist/server/config-schema.js` は4つの位置を受け付け、`config-shared.js` の既定値も既に `'bottom-left'` なのに、実際の描画は右上のままだった（Next 16.2.10）。位置指定を諦めて `devIndicators: false` にした。**本番ビルドにはそもそも出ないので配信物には影響しない**し、コンパイルエラー／ランタイムエラーの表示は `false` でも残る。
  - **踏んだ罠が2つ**: (1) `next.config.ts` は HMR では効かず、dev サーバーの再起動が要る。(2) `pkill -f "next dev"` で旧プロセスが死に残り、新しいプロセスがポート3001に逃げて「再起動したのに設定が変わらない」状態になっていた。`lsof -ti:3000 | xargs kill -9` で確実に落としてから起動する。
  - 対応後、実座標でのタップでメニューが開き、閉じるボタンで閉じることを確認。**「閉じるが効かない」と一度誤診した**——ヘッドレスは毎秒3フレームなので、`exit` 0.14秒の AnimatePresence でも DOM から消えるまで1秒以上かかる（600ms で確認して残っていた／1.6秒後には消えていた）。

- **カード右上の「詳しく見る」ボタンを、カード左上の「出口」の矢印に置き換えた**（依頼者の指示）。
  - **役割が変わった**: 詳細を開くのはカード本体のタップで、矢印は**元の HOME ボタン**（完全リセット、`closeToHome("down", true)`）。表示条件も PC の HOME ボタンと同じにした——セクションに寄っているあいだだけ。すでにホームにいるときに「ホームへ」を出しても何もしないため。**スマホでその状態に入る唯一の経路はマーカーのタップ**で、これは前回「最大ズームはマーカータップ」と決めた導線と一致している。
  - **円の背景をやめた。** 置き場所が惑星の上（ほぼ黒の空〜明るい緑の地面まで振れる）なので、塗りの代わりに線へドロップシャドウを掛けて可読性を出している。チップをもう1枚重ねるより、世界の上の印として読める。
  - **矢印の形は5案を110pxで並べて選んだ。** lucide には隣り合う2つしかない——`ArrowUpLeft` は直線の斜め（方向であって退出ではない）、`CornerUpLeft` は直角（リストの一歩戻る）。**カーブが「出ていく」を作る。** 浅い反りは30pxまで縮むとただの斜め線に見え、尾が水平に寝ると「取り消し」に見える。採用したのは尾が右へ大きく膨らんでから立ち上がる形。
  - 点線の起点（アンカー）は**矢印から独立した不可視の span** に移した。矢印が条件付きになったのと、点線が出るのは「セクションに寄っていないとき」＝矢印が無いときなので、**2つは決して同時に存在しない**。

- **縦オフセットを 0.15 → 0.20 に変更した（基準の取り違えを正した結果）。**
  - 前回の一周実測は「レール上端 = 648px」を基準にしていた。これは**矢印ボタンの上端**で、カード自身の上辺（671px、実測）ではない。矢印はセクションに寄っているあいだしか出ず、そのときは点線のほうが隠れている（`CardLeaderLine` の `hidden`）ので、**点線が指しているマーカーを覆うことはありえない**。障害物として数えるべきではなかった。
  - 671px を基準に測り直した一周20点:

    | share | レールに隠れる | 最悪の y | 空の高さ |
    | --- | --- | --- | --- |
    | 0.15 | 1/20 | 710 | 95px |
    | **0.20** | **2/20** | 744 | **119px** |
    | 0.25 | 11/20 | 793 | 142px |
    | 0.30 | 19/20 | 817 | 166px |

    **折れ点が 0.20 へ動いた。** 0.15 から1点増えるだけで空が24px 広がり、その次の段は9点増える。
  - `cameraLayout.test.ts` の fixture も同じ値に更新し、4つの変異（持ち上げ側／一段強く／PC と同じ／分岐の入れ替え）で落ちることを再確認した。

- **検証**: `npm test` 462件、`tsc --noEmit`・`lint`・`build` すべてクリーン。実座標のタップでメニューの開閉を確認。矢印の見た目は表示条件を一時的に固定して撮影し、条件式自体は `{focused && ...}` として残している（スマホで「寄っているがページは閉じている」状態に自動で入るのが困難なため——マーカーは画面px指定の小さなスプライトで、ヘッドレスから狙い撃つのが安定しない）。

### 出口の矢印をやめ、HOME ボタンをカードの上へ戻した

- **依頼者の判断で矢印を撤回し、元の HOME ボタン（白い錠剤に `×` と HOME）に戻した。** 置き場所はカードのすぐ上・画面中央。
- **見た目は `hub/HomeButton.tsx` に1つだけ持つ。** PC は画面下中央、スマホはカードの上——**違うのは置き場所だけ**なので、位置だけを `className` で受け取り、ラベル・アイコン・形はここに1回書く。表示条件（セクションに寄っていて、かつ詳細ページが閉じている）も両方で同じ。
- **絶対配置にした理由**: フローに入れると、エリアに寄るたびにカードがボタンの高さぶん下がり、抜けるたびに戻る。スライドで済むはずの遷移がレールごと跳ねる。
- **実測**: ボタンは `[129, 600, 132, 50]`、カード上端 664 との間に 21px。画面中央からのずれ 1.5px 未満、`elementFromPoint` がボタン自身を返す（押せる）。

- **レール上端の実測を2回間違えていたので、表を直した。**
  - 1回目は **648**（矢印ボタンの上端）。矢印はセクションに寄っているあいだしか出ず、そのときは点線のほうが隠れているので**障害物ではない**。
  - 2回目は **671**。これは `document.querySelector('[data-ui] .rounded-3xl')` が返す**両隣のカード**で、`scale(0.9)` が掛かっているぶん枠より7px 下に出ていた。真ん中のカード（＝枠）の上端は **664**。
  - 664 で集計し直すと 0.15→1/20、**0.20→3/20**、0.25→11/20、0.30→19/20。**折れ点は 0.20 のまま**（次の段が8つ増える）なので値は据え置き、定数・docstring・テストの fixture だけを正しい数に直した。
  - **教訓**: 「レールの上端」のような**合成された境界**は、それを構成する要素のどれで測るかで答えが変わる。`querySelector` が返す最初の1つを使う前に、**それが変形を受けていないか**を確かめる。

- **検証**: `npm test` 462件、`tsc --noEmit`・`lint`・`build` クリーン。`cameraLayout.test.ts` は4つの変異（持ち上げ側／一段強く／PC と同じ／横寄せをスマホにも）で落ちることを再確認。

### HOME を押すと詳細ページが開いてしまう不具合

- **症状**: スマホでカードの上の HOME ボタンを押すと、ホームへ戻ると同時に詳細ページが開く。
- **原因**: レールのジェスチャ判定（`onPointerDown` / `onPointerUp`）が**レールのルート**に付いていて、バブリングで中身のイベントを全部拾う。HOME ボタンのタップは移動量ゼロなので `isRailTap` が真になり、`onOpen()` が走っていた。ボタン自身の `onClick`（`closeToHome`）も走るので、**開いてから閉じる**という2つの状態遷移が続けて起きていた。
- **修正**: `event.target.closest("button, a")` に当たったプレスは、レールのジェスチャとして扱わない。**コントロールごとに覚えるのではなく DOM に訊く**形にしたので、あとからレールに足したものも自動で対象外になる。
  - シーンの `isInteractive` は同じ歩き方をするが、**ここでは再利用できない**——あちらは `[data-ui]` にも当たり、それはレール自身だから。
- **なぜ設計段階で見落としたか**: カードの上に載せるものが「カードの一部」なのか「カードの上の別のもの」なのかを、**ジェスチャの側が区別する仕組みを持っていなかった**。矢印だった頃も同じ穴が空いていたが、あのときの矢印は「詳細を開く」ボタンで、誤爆の結果が本来の動作と一致していたので**症状として現れなかった**。役割を HOME に変えた瞬間に露見した。
- **検証**: HOME をタップ → シートは開かない（直後・9秒後とも false、hash も変化なし）。カード本体をタップ → 開く（true）。横スワイプは1つ送れる。空タップの停止／再開（差分画素 80,700 ↔ 20）とドラッグでの誤爆なしも再確認。

### カードを薄くし、ドラッグに追従させ、並びを「間を空けた3枚」にした

- **依頼は3つ**: (1) カードをもう少し薄く、角丸を弱く。(2)「スライドしたら次のカードに移るが、**保持**できるように」。(3) 並びを参考画像のようにする。

- **(2) の「保持」を実装前に確認した。** 言語化しづらいという申告があったので、コードから読み取れる候補を3つ出して選んでもらった——(a) スワイプ後も自動周回が止まらず数秒で別のカードへ流れる、(b) ドラッグ中に見た目が動かない、(c) HOME や詳細ページから戻ると位置を失う。**答えは (b)「ドラッグ中の指に追従してほしい」**。
  - **確認して正解だった。** 文面（「次のカードに移るが」）だけ読むと (a) に見え、その線で直すと**アイドル周回を止める**という別物の変更になっていた。アイドル周回はジオラマが生きていることの中身で、止めるかどうかは見た目の手触りとは別の判断。
  - なお現状でも 36px 未満のドラッグは `stepForRailDrag` が 0 を返して**同じカードに留まる**。足りなかったのは判定ではなく、離すまで何も動かないという**無反応**のほうだった。

- **追従は「見た目だけ」に閉じ込めた。** 3枚を包む transform 専用の `motion.div` を1枚足し、`useMotionValue` を `style={{ x }}` に直結して pointermove ごとに書く。離したら同じばね（`RAIL_SPRING`、カードのスロット移動と共有）で 0 へ戻す。
  - **カメラはこの値を読まない。** 読ませた瞬間、却下済みの「スクロールコンテナ」と同じ閉ループになる（`docs/scene-invariants.md`「カードとカメラを相互に更新しない」）。送るかどうかは従来どおり離した瞬間の `stepForRailDrag(dx, dy)` だけが決め、追従量は判定に入らない。
  - **state ではなくモーション値**にしたのは、pointermove ごとの `setState` が1ドラッグでレールを60回再描画するから。既存の `dragStart` が ref なのと同じ理由。
  - **包む `motion.div` に `absolute inset-0` を明示した。** `x` が付くと transform ありの要素になり、中の `absolute inset-0` の**含みブロックになる**。箱を持たせないと高さ auto（中身は絶対配置なので0）に対して `top:0;bottom:0` が解決し、**カードが高さ0に潰れる**。
  - **縦優位のドラッグでは追従させない。** `stepForRailDrag` が縦優位を拒む理由（ジオラマへ向けたジェスチャ）は見た目にもそのまま効く。
  - 追従量は隣のカードの位置で頭打ちにした。窓は3枚しかないので、それ以上引くと**4枚目ではなく空白**が出てくる。

- **(3) 並びを重ね（0.84）から間空け（1.082）へ。** 参考画像は両隣が中央カードに**乗っていない**。
  - **px ではなく比で導いた。** 参考画像のカードは画面の85%、こちらは78vw なので、隙間の px をそのまま持ってくると見た目が一致しない。参考では両隣が 780 の画面に対し 35.5 見えている＝**4.6%**、390px なら 18px で、これはカードの padding（16px）の内側——だから**文字が出ず白い縁だけ**になる。逆算して隙間 43 − 18 = 25px、25/304 で **1.082**。
  - **両隣の `scale(0.9)` と `opacity: 0.45` も外した**（参考画像では同じ大きさ・同じ白）。どれが説明中かは間隔とドットが言う。副次的に、`scale` で両隣が枠より7px 下に出る——レール上端の実測を1度狂わせたあの罠——が消えた。
  - `railLayout.test.ts` の2件を「重なること」から「**隙間が空くこと**」「隙間が広すぎて画面から消えないこと」へ書き換えた。

- **(1) 薄く・角丸を弱く**: `rounded-3xl`→`rounded-xl`、`shadow-xl shadow-black/20`→`shadow-md shadow-black/10`。高さ（`CARD_HEIGHT = 132`）と padding は触っていない——コピーの行数に合わせて詰めてある値なので、影と角だけで「薄さ」を出した。ばねの定数は `RAIL_SPRING` に1つへまとめ、カード側と戻り側で同じ数を2箇所に持たないようにした。

- **ヘッドレス検証の罠（新規）**: `page.evaluate` で位置を刻んでもばねが**1pxも進まない**。`isAnimating()` は true のまま。原因は**ヘッドレスでは rAF がフレームを要求されたときしか回らない**ことで、`evaluate` はフレームを起こさない。`page.screenshot` を挟むと進む。**モーションの検証はスクリーンショットで刻む**こと。危うく「framer-motion の `animate()` が動かない」と誤診しかけた。
- **検証**: 390×844 で、静止（両隣は白い縁だけ・中央カードとの間に隙間）、ドラッグ中（3枚が指と一緒に左へ、右から次が出てくる）、離したあと（ばねで整列し、`stepForRailDrag` の送りが成立して 経歴・活動 へ、ドットも3番目へ）をそれぞれ撮影して確認。`npm test` 462件、`tsc --noEmit`・`lint` クリーン。

### 技術スタックのアイコンを、スマホではタップで点灯させる

- **症状**: スマホだと Skills のアイコンが**全部グレーのまま**で、ブランド色が一度も出ない。
- **原因**: 色を出しているのは `group-hover:text-[var(--hover-color)]` だけ（枠線・拡大・ラベルの濃さも同様）。**ホバーの無い画面には出口が無かった**。PC ではポインタが常にどこか1つの上にあるので問題として現れない。
- **修正**: `useHandheld()` が真のときだけ `onClick` を生やし、押されたタイルを `litSkill` として持つ。**PC 側の `hover:` は一切触っていない**——スマホで具合が悪いからといって PC の値を書き換えない、の通り。判定は既存のフック1つで、2つ目の述語は作っていない。
- **同時に点くのは1つだけ**にした。ホバーが1つずつなのはポインタが1つだからで、タップで次々に点いて**残る**と、読み進めるほど画面がスクラッチカードになる。同じタイルをもう一度押すと消える。
- **`<button>` にはしなかった。** 情報はラベルの文字で既に出ていて、点灯は装飾。ボタンにすると読み上げが「何も起きないボタン」を30個読むことになる。
- **検証**: 390×844 で `#skills` を開き、計算済みスタイルで確認——タップ前は3枚とも `lab(65.9 …)`（`text-gray-400`）、TypeScript をタップすると `rgb(49, 120, 198)`（= `#3178C6`、定義どおりのブランド色）になり**他の2枚はグレーのまま**、もう一度タップで戻る。枠線・ラベル・拡大も同時に効くことをスクリーンショットで確認。`tsc --noEmit`・`lint` クリーン。

### スマホの視点を2段とも引いた——「惑星の縁が画面の3分の1」を1つの規則にした

- **依頼**: 俯瞰視点は今よりもう少し俯瞰に。標準視点はもう少し俯瞰にしたうえで**画面の3分の1は背景が見える**ように（惑星の位置を下げたい）。

- **最初に PC 側を変えてしまった。** 依頼文に画面の指定が無かったので両方に効く定数（`ORBIT_RADIUS` / `NEAR_ORBIT_RADIUS`）を動かしたが、**話はスマホ側だった**。PC は元のまま（100 / 50、参考写真由来）に戻し、スマホ用の高度を別に足す形にやり直した——CLAUDE.md の「PC 用の定数をスマホの都合で書き換えず、スマホ用の定数を別に足す」を、**逆向き（スマホの都合で PC を動かす）にやっていた**ことになる。`nearVerticalShare(handheld)` と同じ構造がすでにあったのだから、最初からそちらに乗せるべきだった。
  - **どちらの画面の話かは、依頼文に書かれていなくても確定させてから触る。** 構図の依頼は「どのフレームの話か」が決まらないと値が決まらない（PC は16:9、スマホは0.46で下端にカードレールがある）。

- **スマホの高度は1つの規則から解いた: 惑星の縁が画面の3分の1の位置に来る（`HANDHELD_PLANET_TOP = 1/3`）。** 惑星の中心は注視点そのものなので画面の `0.5 + lean/2` に来て、縁はそこから円盤の半分だけ上。つまり必要な円盤の高さは `1/3 + lean` で、**俯瞰はリーンが無いので 1/3、標準は `1/3 + HANDHELD_VERTICAL_SHARE`**。2段の違いはリーンの有無だけになる。
  - 距離に直すのは `orbitRadiusForDiscHeight(share)`（円盤の直径は画面高の `tan(asin(R/D))/tan(fov/2)`、その逆関数）。**PC 側はこの式を使わない**——あちらは参考写真が距離を直接指しているので、解き直すと誰も頼んでいない構図の変更になる。
  - 結果: 標準 50 → **77.9**、俯瞰 100 → **122.8**（スマホのみ）。`orbitRadiusForZoom(zoom, handheld)` が選ぶ。`handheld` は既定値を持たせず必須にした——**渡し忘れたらスマホが PC の構図で描かれる**という、この定数が存在する理由そのもののバグになるため。

- **390×844 の実測**（`docs/verification.md` の「幅の半分以上が背景でなくなる最初の行」＝水平線）:

  | 段 | 惑星の縁（式） | 水平線（実測） |
  | --- | --- | --- |
  | 標準 | 143 → 281px | **125px (14.8%) → 267px (31.6%)** |
  | 俯瞰 | 248 → 281px | **250px (29.6%) → 279px (33.1%)** |

  標準は空が画面の15%しか無かったのが**ちょうど3分の1**になった。俯瞰はもともと3割近くあったので動きは小さい。

- **リーンは触っていない。** 同じことは `HANDHELD_VERTICAL_SHARE` を上げてもできるが、スマホのリーンの上限は好みではなく**カードレール**で決まっている（0.20 が折れ点、0.25 で一周20点中11点が隠れる）。引くほうを選ぶと空が開くうえに押し下げ量 `lean·radius·H/(2·depth)` は**縮む**——半径は +56%（50→77.9）だが深さは +142%（19.6→47.5）なので、押し下げは 215px → 138px——ので、点線は悪くなるどころか良くなる。`cameraLayout.test.ts` にこの不等式をピン留めした。

- **検証**: 390×844 で標準・俯瞰の両方を撮影（俯瞰へは**実際のピンチ**——`pointerdown` 2本を canvas に投げて距離を160→80に縮める——で入り、ジェスチャ経路ごと確認）。PC（1440×900）は変更前と同じ構図に戻っていることを実測で確認（球の上端 323px → 324px、街込み 273px → 275px、差はアイドル回転ぶん）。`tsc --noEmit`・`lint`・`npm test` 470件クリーン。

- **踏んだ罠**:
  - **zsh は `set -- $c` で語分割しない。** 候補値を `"58:0.66"` のペアで回して `set -- $c` を使ったら `$1` に `58 0.66` が丸ごと入り、`sed` が `export const NEAR_ORBIT_RADIUS = 58 0.66;` を書いた。dev サーバーはパースエラーになり、**撮れた3枚は全部エラーオーバーレイ**だった。`${c%%:*}` / `${c##*:}` に変えた。
  - **ヘッドレスの PNG を読む手段が無い。** この環境には PIL も sharp も無いので、`zlib` で PNG デコーダ（フィルタ0〜4）をスクラッチパッドに書いた。行ごとに非背景画素を数え、**幅の半分**を超えた行を水平線（スマホ）、**幅の1割**を超えた行を球の上端（PC）とした。
  - **構図をピクセルで測るときは DOM を消してから撮る。** ヘッダー・カード・点線の SVG を `visibility: hidden` にしないと、横に伸びる点線が1行で300px以上の非背景画素を作り、上端を誤判定する。ヘッダーのロゴも「惑星の上端」を毎回 y=28 と答えさせていた。
  - **点線の終端座標は隠れているあいだ更新されない。** `<path>` の `d` から終端の y を拾うと非表示区間では前の値が凍る。可視性は `<g>` の `opacity` で数える。

- **PC 側で測った表も残しておく**（今回は採用しなかったが、同じ依頼が PC に来たときの出発点になる）。1440×900・ツアー u=0 の同じ弧で、背景の帯は球の上端／街を入れた上端:

  | 半径・リーン | 球の上端 | 街を入れた上端 |
  | --- | --- | --- |
  | 50, 0.636（現行） | 323px (35.9%) | 273px (30.3%) |
  | 58, 0.66 | 398px (44.2%) | 341px (37.9%) |
  | 60, 0.72 | 437px (48.6%) | 375px (41.7%) |
  | 64, 0.80 | 496px (55.1%) | 435px (48.3%) |

  一周20点の実測では、半径56.6・リーン0.636 で背景の帯は球の中央値42.1%・街を入れて32.9%（最小16%＝高い塔が縁に立つ瞬間）。点線が描かれたのは20点中3回で、**引いても点線は戻らない**（A-3 の `share·radius·H/(2·depth)` のとおり）。

### 2本指ジェスチャを、PC と実機スマホの両方で成立させた

- **報告された症状は「PC と実機で結果が逆」の2つ**: (1) 2本指ズーム（俯瞰⇄標準）は**実機スマホでは効くが PC 検証では効かない**。(2) 2本指スクロール（軌道の飛行・自己紹介文のスクロール）は**PC では効くが実機スマホでは効かない**。

- **原因は「入力の経路が片方の環境にしか無い」こと。** 同じ「2本指」でも、届き方がまったく違う。
  - **PC のトラックパッドのピンチは pointer イベントではなく `wheel` で来る**（`ctrlKey: true` 付き。ブラウザがページズームに使う流儀）。`useViewInput` のピンチは `pointerdown` が2つ来ることを前提にしていたので**発火しようがなかった**。しかも素通りして通常のホイール処理に落ちていたので、**ピンチするとツアーが進む**という誤動作になっていた（無反応より悪い）。
  - **スマホには `wheel` が無い。** ツアーの前進も自己紹介のスクロールも `wheel` にしか繋がっていなかったので、指では動かしようがなかった。2本指は「ピンチかどうか」だけを見て早期 `return` していたため、**平行移動は完全に無視**だった。
  - **自己紹介はもう一段深い。** コラムは `overflow-y-auto` の実スクロール要素なので指では動く——が、`scrollTargetRef` を正とする毎フレームの追従ループ（`scrollCatchUp`）が **次のフレームで引き戻していた**。「ホイールは preventDefault しているので他に動かすものは無い」という前提がタッチで崩れていた。無言で失敗する形（テキストがただ動かない）だったのが厄介。

- **直し方は3つ、どれも「もう片方の経路を足す」形にした。**
  1. **2本指に2つのモードを持たせた**（`twoFingerGesture()`）。指の**間隔が変われば**ズーム、**一緒に動けば**ツアーの飛行。最初に決まったほうがその手離しまでジェスチャを保持する（ラッチ）。
  2. **ctrl+wheel をピンチとして拾う**（PC のトラックパッド）。小刻みなデルタの列なので合計が `PINCH_WHEEL_THRESHOLD` を超えたら1回だけ段を切り替え、220ms 静かになるまで再発火しない。`preventDefault` はブラウザのページズームを止めるために必須（`userScalable: false` はこのジェスチャを止めない）。
  3. **自己紹介は「自分が書いていない `scrollTop`」を採用する。** 追従ループが書いた値を `writtenTopRef` に控えておき、`handleScroll` でそれと1px以上ずれていたら**指が動かした**と判断して `scrollTargetRef` をそこへ移す。指1本でも2本でも、ブラウザの慣性も、そのまま効く。

- **ズームとスクロールの判定は「どちらが先に閾値を超えたか」ではなく「どちらが大きいか」。** 片手の親指を固定して行うピンチは、間隔が変わる量の**半分だけ中点も動く**。先着順にすると、間隔が60pxのうち48pxしか開いていない時点で中点が24pxに達して**スクロールと誤判定**する（＝片手ピンチで高度が切り替わらない）。`|spread|` と `|travel|` を直接比べる形にしてある。この awkward case はブラウザで指2本を再現しながら詰められないので、`viewGesture.ts` に純関数として切り出し、`viewGesture.test.ts` に「片手ピンチ」「平行スワイプ」「途中でどちらでもない」を置いた。

- **2本指スクロールの速さは `DRAG_SENSITIVITY`（0.002 rad/px）＝1本指ドラッグと同じ率にした。** ホイールの `WHEEL_SENSITIVITY`（0.0004）をそのまま使うと、トラックパッドの慣性が無いぶん**790px 引いてツアーの1/20**という体感になる。同じ指が同じ距離動いたら世界も同じだけ動くべきで、違うのは**どの経路を動くか**だけ。

- **「建物正面視点」はピンチの巡回に入れていない**（依頼どおり）。`onPinchZoom` は `setOrbitZoom` だけを呼び、セクションに寄っている間は `useViewInput` 全体がロックされる。実測でも、俯瞰でもう一度つまんでも俯瞰のままだった。

- **検証**（すべてヘッドレスの実測）:
  - PC 1440×900: 標準 →（ctrl+wheel でつまむ）→ **俯瞰** → もう一度つまんでも**俯瞰のまま** →（広げる）→ **標準**。通常のホイールは従来どおりツアーの飛行（停止状態で前後を比べて画素の30.9%が変化、段は不変）。
  - スマホ 390×844: 空タップで停止 → **何もしない4秒で画素の変化 0.0%** → 2本指で上へ510px → **47.8%が変化**（＝飛んだ）。ピンチで俯瞰／標準を往復。
  - 自己紹介: コラムの `scrollTop` を 9 → 309 に動かす（ブラウザが指で行うのと同じこと）と、2.5秒後も **313**。従来はここが引き戻されて動かなかった。
  - `tsc --noEmit`・`lint`・`npm test` 476件クリーン。

- **踏んだ罠**: **合成した `PointerEvent` には `offsetX` が生えない**（コンストラクタが `clientX` しか受け付けない）ので、R3F のレイキャストに乗る操作——空タップでの停止など——は `Object.defineProperty` で足してから投げる必要がある。`docs/verification.md` に既出の罠だが、今回の停止ジェスチャでも同じところを踏んだ。
