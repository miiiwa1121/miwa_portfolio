# 開発ログ (2026年7月)

## 2026-07-18
### 3Dジオラマポートフォリオへのアーキテクチャ刷新
- **デザイン方針の大幅な転換**
  - 参考サイト(`haru-ni.net`)をベースに、ポートフォリオ全体を「カメラが周回するボクセル空間」にリデザインする方針を決定。
  - 初期の実装をクリーンにリセットし、新たな `feature/3d-redesign` ブランチにて開発を開始。

- **プロシージャル・ボクセルジェネレーターの実装**
  - 外部のGLBモデルに依存せず、コードと数式（サイン波・距離計算）のみで浮遊島（アイランド）を生成するアルゴリズムを構築。
  - Three.js の `InstancedMesh` を活用し、数千個のブロックを描画しつつも 60fps を保つ高パフォーマンスな描画を実現。草・土・岩の深さに応じた色分けを適用。

- **インタラクションとカメラ制御の高度化**
  - `OrbitControls` から `@react-three/drei` の `CameraControls` へ移行。
  - 光るモノリス（各セクションのアクセスポイント）をクリックした際、カメラが対象へスムーズにズームイン（Swoop in）するダイナミックな演出を追加。
  - 画面下部に「HOME」ボタンを実装し、元の全体ビューへ戻るアニメーションを構築。

- **ハイブリッド・スクロールレイアウトの確立**
  - `<Canvas>` をページの背景（`position: fixed`, `z-index: -1`）に固定。
  - トップに `100vh` の透過スペーサーを配置し、そこを3D空間の操作エリアとして確保。
  - マウスホイールで下にスクロールすると、通常のHTMLセクション（About, Products, Skills等）が下からオーバーレイしてくる構造を実装。没入感とアクセシビリティを両立させた。

### パステル・ボクセルシティへのデザイン変更（New!）
- ユーザーからの具体的なデザイン要望に基づき、世界観を「暗いサイバー空間」から「明るくポップなパステル空間」へと大幅に変更。
- **UIオーバーレイの完全実装**:
  - 左上に「App Factory はる」のロゴとオレンジの採用ボタンを配置。
  - 右上にグローバルナビゲーションを配置。
  - 左側に「会社概要」の白いカードを配置し、中央にはターゲットカーソル（Crosshair）を固定。
  - 画面右下には言語切り替え用のトグルボタン（地球儀アイコン）を配置し、既存の言語コンテキストと接続。
- **3D空間のリペイントとプロトタイプ配置**:
  - ライティングを暖かみのある太陽光（`#fffaeb`）に変更し、背景色もパステルイエロー（`#fffdf0`）に統一。
  - 地面を「黄色い平面に緑色のグリッド（道路）」に変更。
  - 外部の `.glb` モデルが導入されるまでのプロトタイプとして、指定された建物の「当たり判定と仮組み」を実装。
    - 黄色い観覧車（`FerrisWheel`）
    - ピンクのGAME DEVELOPビル（`PinkBuilding`）
    - 緑のIT ADVENTUREビル（`GreenBuilding`）
    - 会社のゲート（`CompanyGate`）
  - 全体を明るく楽しい雰囲気にし、完成に向けた最強の土台（レイアウトとインタラクション）を構築完了。

- **ドキュメント整備**
  - `docs/tech.md` を作成し、現行の技術スタック（Next.js, R3F, Framer Motion等）を整理・記載。
  - 本開発ログ (`docs/devlog/202607.md`) の運用を開始。

## 2026-07-19
### プロシージャル・ボクセルエンジンへの全面移行（外部モデル不要）
- **方針**: MagicaVoxel等の外部制作に頼らず、コードだけで `haru-ni.net` 風のパステル・ボクセルシティをどこまで再現できるかを実装。単純な `boxGeometry` の寄せ集めだった建物を、本物のボクセル解像度へ引き上げた。

- **ボクセルエンジンのコア (`src/components/3d/voxel/`)**
  - `VoxelModel.tsx`: 数千個の小キューブを **1つの `InstancedMesh` + インスタンスごとの色**で描画（1モデル1ドローコール）。60fpsを維持。
  - `builders.ts`: `fillBox` / `shellBox` / `carve` / `paint` / 簡易ノイズなど、ボクセル配列を手続き生成するヘルパー群。
  - `text.ts` / `VoxelText.tsx`: 任意の文字列をオフスクリーンCanvasでラスタライズし、**フォント→ボクセル文字**へ自動変換（"GAME"/"PRODUCTS"等の立体看板をコード生成）。
  - `palette.ts`: 世界観を統一するパステルパレットを一元管理。

- **街の構築**
  - `VoxelIsland.tsx`: 草／土／石の層と、ピンクの環状道路・十字路・中央広場を持つ**浮遊島**を数式で生成（先細りの底面）。
  - `ProceduralObjects.tsx`: 5セクションのアンカーをボクセル建築で再構築（Aboutの家、Products のピンク・ランドマークタワー、Skills の青いタワー、Experience の図書館、Contact の看板）。ホバーで浮上、クリックでカメラズーム。ラベルは `Billboard` で常時カメラを向く。
  - `Decorations.tsx`: 観覧車（回転）、木、街灯、雲、歩き回るNPC、降り注ぐパステル紙吹雪（`Confetti`）を追加。
  - `VoxelBus.tsx`: 環状道路を周回するボクセル・トラムに刷新。

- **UIオーバーレイの自分ごと化**
  - 借用していた「App Factory はる」等のコピーを全て撤去し、Miiiwa自身の情報（bio・SNS・モットー「面白いを最優先！」）へ差し替え。日英切り替え対応。
  - セクションに応じて内容が切り替わる左カード、`HOME` ボタン、隠しターミナル起動ボタンを実装（`TerminalOverlay` の描画を復帰）。

- **検証**: `next build`（静的エクスポート）成功。ヘッドレスChrome + SwiftShader で実描画をスクリーンショット確認し、ズーム／カード切替／HOMEボタン等のインタラクションが動作することを確認（コンソールエラーなし）。

## 2026-07-27
### 技術選定の再検討と、旧ポートフォリオ資産の一掃
- **Nuxt への移行を検討し、見送り**
  - 参考サイト `haru-ni.net` の開発者が Nuxt を使っていると聞き、技術選定をゼロから見直した。結論は「乗り換えない」。
  - 理由: 惹かれている表現（ボクセルの街、カメラのスウープ）は Three.js とデザインの成果で、Nuxt/Next は外側のページシェルに過ぎない。`output: 'export'` の静的サイトなのでデプロイ面の差もゼロ。むしろ R3F + drei に対し Vue 側の TresJS はヘルパー層が薄く、**3D 側のダウングレードになる**。

- **依存の宣言漏れを修正（クリーンクローンでビルド不能だった）**
  - `three` / `@react-three/fiber` / `@react-three/drei` が `src/` から import されているのに `package.json` にも `package-lock.json` にも記載がなかった。ローカルの `node_modules` にだけ存在していた状態。
  - インストール済みバージョンで固定して宣言。scratchpad に lock だけコピーして `npm ci` を通し、再現性を実測で確認。

- **旧ポートフォリオの残骸を削除**
  - 到達不能な死にコード: `Header.tsx` / `SidePanel.tsx` / `sections/Hero.tsx` / `InteractiveParticles.tsx` / `AnimatedBackground.tsx`。
  - 未使用アセット: create-next-app のボイラープレート SVG 5点、`me0.png` / `me1.png` / `me2.mp4`。

- **パステル移行の積み残しを完了（表示上のバグだった）**
  - 詳細ページの土台は `bg-white` なのに、中身のセクション群が旧ダークテーマのまま（`text-white` / `bg-white/5`）で、**見出しが白背景に白文字で不可視**、カードも輪郭ごと消えていた。スクリーンショットで確認。
  - `globals.css` のネオンシアン/パープルを、ボクセル世界の `palette.ts` と対応するオレンジ/ピンク/グリーンへ差し替え。Skills / Products / Experience / Contact と共通の `Section` 見出し、Products の詳細モーダルを白カード＋オレンジアクセントへ統一。
  - README が「インタラクティブなパーティクル」「ダークモード & グラスモーフィズム」を売りに書かれたままだったので全面改稿。`docs/tech.md` にデザイントークン表を追加。パッケージ名を `temp-app` → `miiiwa-portfolio`。

## 2026-07-28
### 詳細ページの出現・退場をシート化
- **下からスライドで出現**: フェードをやめ、1画面ぶん下から springで せり上がる動きへ。
- **上下どちらへ抜けてもホームに戻る**: 従来は下スペーサーのみだったので、パネルの上にも鏡像でスペーサーを追加。開いた時にスクロール位置をパネル先頭へ合わせる処理が `scroll` イベントを発火させ「一番上＝ホームへ戻れ」と誤判定される問題は、1フレーム分のゲートで解決。
- **退場方向をスクロール方向に追従**: 下端から閉じるとパネル下端が画面上端にあるため、下向きに退場させると `shadow-2xl` が画面を縦断していた（実測: パネル下端が画面上端の5px下）。離脱方向へ抜けるよう変更。判定は純粋関数に切り出してテスト。

### 報告された3つの不具合を修正
- **開く時のカクつき**: 計測の結果、原因は2つ。
  - Skills を開くと `cdn.jsdelivr.net` へ **28リクエスト**。各スキルは最初からローカルの react-icons を持っていたのに、リモートURLが優先されていた。→ 28 → 0。
  - Products の画像が **16MB の生 PNG**（`imadoko.png` は 6.8MB / 3420px を高さ192pxのカードに表示）。静的エクスポートのため `next/image` は `unoptimized` 強制でリサイズが効かない。sharp で 1600px / WebP へ再エンコードし **16MB → 337KB（97.9%減）**。
- **戻る境界の曖昧さ**: スペーサーが 150vh、ビューポートが 100vh。パネルが画面から消えるのは progress 0.67 の時点で、**残り半画面ぶんが死に区間**だった。スペーサーを1ビューポートに揃え「消える瞬間＝戻る瞬間」を一致させた。慣性で数px手前に止まると二度とイベントが来ないため閾値 0.98、開いた直後のロックは 1200ms → 450ms。
- **視点が2〜3回切り替わる**: `camera-controls` の `_createOnRestPromise` は呼び出しの識別子を持たず、次の `rest` で誰の Promise も解決する。2つの effect が1個の boolean を共有していたため、古い飛行の `.then` が飛行中にフラグを倒し、`useFrame` が毎フレーム上書きを始めていた（1台のカメラに2人の書き手）。飛行ごとにトークンを発行して解決。あわせて、タブ切替時に「ホームの角度」を建物視点の角度で上書きしていたバグも修正。

### コード品質の底上げ（5項目）
- **3D装飾の決定論化**: 紙吹雪が `useMemo` 内で `Math.random()` を呼び、そのメモ化配列を `useFrame` が毎フレーム破壊的に書き換えていた。粒子状態を ref へ、スクラッチ変換をモジュール定数へ、生成を決定論的ハッシュ（`voxel/rng.ts`）へ。lint エラー10件が解消。
- **カメラ構図の一本化**: 建物構図とホーム構図が3箇所に手書きコピーされていた。`Pose` タプルを返す純粋関数2つに集約。毎フレームの `Vector2` アロケーションも削除。
- **Products の分割**: 325行 → 98行（合成のみ）。`products/` 配下へフィルタ・カード・モーダル・トーストを分離。移動中にトーストのタイマー未クリア、`setTimeout(0)` による lint 回避、モーダルの Escape/role 欠如が判明し修正。
- **データ構造の是正**: 全プロジェクトが `{ ja, en }` の二重管理だったが、実際に異なるのは title と description だけで、残り7フィールドは同一のコピー。`Localized<string>` へ再構成し、8ファイル＋バレルを1つの `PROJECTS` 配列に統合（追加時の編集箇所が3→1）。
- **テスト導入**: Vitest。React も three.js も含まない純粋ロジックのみを対象にし node 環境で実行。そのために `worldLayout.ts`（座標とカメラ構図）と `confetti.ts`（粒子シミュレーション）を描画から分離。**緑になったことを信用せず変異テストで検証**し、6つの意図的なバグのうち5つを捕捉、すり抜けた1つ（`sectionPose` の内向き反転）はアサーションが意図を表現できていなかったため書き直した。

### カメラモデルの作り直し（参考サイトの記事に基づく）
- 開発者本人の解説記事を読み、以下が判明。
  - **層の分け方**: 拍動する円形アイコンは WebGL（他の3Dオブジェクトに隠れる）、点線コネクタは DOM、ツールチップは DOM 最前面。
  - **スクロール同期のカメラパスは一度作って断念**（3D酔いの懸念）。最終形は単純な周回。
  - **FPSガバナー**: 操作中60fps / 静止30fps / **canvas がコンテンツの裏に隠れている時 0.5〜1fps**。
- **スクロール連動補間を削除**: 白い不透明パネルの裏で毎フレーム `lerpLookAt` を回していた。誰にも見えず、かつ「分岐は state・入力は ref」の時間差により視点バグの原因そのものだった。
- **FPSガバナー実装**: パネルが画面を覆いきってから `frameloop="demand"` + 1秒ハートビート。実測（WebGLドローコール計測）で覆われている間 **毎秒1描画に固定**、スペーサーで復帰することを確認。
- **慣性の流入を遮断**: 強くフリックして閉じるとカメラが回り出す症状。慣性スクロールは指を離した後も1〜2秒イベントを出し続け、ロック解除直後の残りが回転に化けていた。「ロック中に始まったストリームは、ホイールが静かになるまで受け付けない」方式で解決。
- **戻り先の仕様変更**: 「離れた時の角度を記憶して戻る」を廃止。スクロールで離脱 → **全景の距離のまま、そのエリアが正面に来る角度へ**。ロゴ/HOME → 毎回同じ固定視点。カメラの行き先を決める処理を1つの effect に統合。

### カードとマーカー
- **カードをカメラから導出**: `activeSection` 依存をやめ、周回の方位角から「今どのエリアが正面か」を求めて表示。カードがカメラを動かし、カメラがカードを変える双方向は必ず喧嘩するため一方向にした。実測で、周回に従いカードが順に切り替わること、技術スタックをスクロールで閉じた直後に「技術スタック」カードが出ることを確認。
- **エリアマーカー**: 各エリア上空に脈打つドットをスプライトで配置。深度バッファに乗るため建物が手前にあれば自然に隠れる。正面のドットはアクセントカラーで拡大。アンカー高さは現行の建物実寸に合わせた手調整値（モデル変更時に要調整）。

### カード → マーカーの点線（DOM）
- **層の分担**: マーカーは WebGL（建物に隠れてほしい）、点線は DOM（常に最前面の平面）。参考サイトの記事と同じ分け方。
- **毎フレームの受け渡しを React state に通さない**: シーンが持つカメラ情報を DOM 側が必要とするが、state 経由にすると60回/秒の再レンダーが走り、描画と同じメインスレッドを奪い合う。小さな購読チャンネル（`3d/markerScreen.ts`）を作り、シーンが push、線側は SVG の属性を直接書き換える方式にした。
- **描画**: 20個の `<circle>` をカードの角からマーカーまで二次ベジェ上に配置。半径を 4.5px → 1.8px へ先細りさせ、弦に対して垂直方向に少し反らせて弧にしている。
- **可視判定**: 当初はカメラ前後（`z < 1`）しか見ておらず、マーカーが画面上端の外（実測 y = -238）に出ると線が画面外へ飛んでいた。ビューポート内かどうかも条件に追加。

- **判明した制約**: 全周を回して実測したところ、**5エリア中「制作実績」だけ点線が常に非表示**。ピンクタワーが高く、マーカーがホーム視点の画面上端より上に出るため。マーカー高さを 13.3 → 10.4（アンテナではなく塔屋を基準）に下げても解消せず、**原因はマーカーではなくホーム視点の画角そのもの**（現在の `HOME_RADIUS = 24` では高い建物が上で見切れている）と確定。次の A1（`fitToBox` によるエリア単位の構図決定）で対応する。

### ホーム視点の画角を実測から決め直し
- **実測**: シーンに一時的な計測コンポーネントを差し込み、ワールドと各エリアのバウンディングボックスを取得。
  - ワールド全体 y -7.3 〜 22.3（上限は紙吹雪と雲で、構図の制約にすべきでない）、XZ はおよそ ±14。
  - 建物の高さ: 制作実績 **12.2**（アンテナ先端）、技術スタック 7.5、自己紹介/お問い合わせ 4.6、経歴・活動 3.8。
- **原因の特定**: 従来はカメラ高さ11・注視点 y=1 で、俯角 22.6°。垂直画角45°に対し**視野の上端が水平から0.1°**しかなく、カメラの目線より高いものは原理的に写らない。ピンクタワー(12.2)は必ず切れていた。
- **修正**: 注視点を地面(y=1)ではなく**コンテンツの中央(y=3.5)**へ。俯角16.1°となり塔が収まる。半径は 24 → 26。角度計算をスクリプトで検証してから適用した。
- **初回マウントでホーム構図が適用されていなかった**: `<Canvas camera>` は位置しか設定できず注視点は原点のままだったため、起動直後は上記の修正前より悪い俯角で回っていた。マウント時にトランジションなしでホーム構図へスナップするよう修正。
- **マーカー高さの手調整を廃止**: 建物の実バウンディングボックスの上端＋クリアランスで実行時に決める方式へ。建物が育っても屋根に埋まらない。
- **結果**: 全周を実測し、**5エリアすべてで点線が表示**されることを確認（従来は制作実績のみ常時非表示）。

### エリア単位の構図をサイズから計算（`fitToBox` は使えなかった）
- **`fitToBox` を採用しようとして撤回**: 実装を読むと冒頭で `roundToStep(theta, PI_HALF)` によりカメラ角度を90°刻みに丸め、正面・真横・真上のいずれかへスナップする。ジオラマの斜め俯瞰が壊れるため使用不可。API の説明だけを見て推奨したのは誤りだった。
- **代替として自前の純粋関数を用意**:
  - `frameDistance(radius, fov, aspect)`: 対象の外接球が収まる距離。垂直・水平の**狭い方**の半画角で計算するため、縦長ビューポートでも収まる。従来は全建物が距離13固定で、12単位のタワーと4単位の図書館が同じ扱いだった。
  - `framePose(target, azimuth, distance, tilt, sideways)`: そのエリア固有の方位角から、指定距離・俯角で見る配置を返す。`sideways` は注視点を横へずらすもので、**被写体を画面右へ寄せて左のカードを避ける**。
  - `aimOffset(distance, fov, aspect, share)`: 避ける量を「フレーム幅の何割か」から算出。距離が変われば必要量も変わるため定数では足りない。
- **検証**: 変異テスト2種（`sin` → `tan` の定番ミス、狭い方ではなく広い方の半画角を使う）で「被写体が実際に画角に収まる」テストが落ちることを確認。テスト計86件。
