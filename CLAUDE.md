# Miiiwa Portfolio — 作業指示書

学生エンジニア「Miiiwa」の個人ポートフォリオ。トップページは HTML ではなく Three.js で手続き的に生成したボクセル都市で、その上に通常の HTML で組んだ詳細ページを重ねるハイブリッド構成。`output: 'export'` の完全な静的サイトなので、サーバー機能（Route Handlers / Server Actions / ISR など）は使えない。

**応答もドキュメントも日本語で書く。** 英語にするのはコード内の識別子とコミットメッセージだけ。

## あなたの役割

**プロダクトの開発と、docs の作成の両方を担う。** コードを書いて終わりではなく、その変更が `docs/` に反映されているところまでが1つの作業。「あとでまとめて書く」はしない。**都度更新する。**

## この Next.js は既知のものと違う

Next.js 16 には破壊的変更が入っている。API・規約・ファイル構成が学習データと食い違っている可能性がある。**Next.js に関わるコードを書く前に、必ず `node_modules/next/dist/docs/` の該当ガイドを読むこと。** バージョンに一致したドキュメントが同梱されており、そちらが正。非推奨の警告も無視しない。

## コマンド

```bash
npm run dev          # 開発サーバー (:3000)
npm run build        # 静的書き出し → out/
npm test             # Vitest 一回実行（438件で1秒未満）
npm run test:watch
npm run lint
```

## docs は都度更新する

コードを変更したら、**同じターンのうちに**該当する docs を更新する。どれを触るかは変更の中身で決める。

| ファイル | 何を書くか | 更新するタイミング |
| --- | --- | --- |
| `docs/devlog/YYYYMM.md` | 作業の記録と判断の理由 | **毎回**（例外なし） |
| `docs/tech.md` | 技術スタック、設計方針、アーキテクチャの要点 | 構成・依存・設計判断が変わったとき |
| `docs/voxel-object-creation.md` | ボクセル制作のワークフローと規約 | 制作フローや規約を決め直したとき |
| `docs/planet-migration.md` | 惑星への移行の記録（**完了済み**）。仕様と経緯・却下案・実測値 | 通常は更新しない。実装済みの構造は `tech.md` に移した |
| `docs/review.md` | 未修正の指摘の台帳（2026-08-02 の全体レビュー）。問題点＋解決策＋再現手順 | 指摘を直したら該当項目を消す。恒久的な設計判断になったものは `tech.md` へ移す。**新しい設計をここに書かない** |
| `README.md` | サイトの特長、起動手順、ディレクトリ構成 | ユーザーから見える機能や手順が変わったとき |
| `CLAUDE.md` | 作業の前提とルール | 守るべき不変条件やルールが増減したとき |

### devlog の書き方

`docs/devlog/YYYYMM.md` は git の履歴とは別の、プロジェクト自身の記録。あとでまとめて書かない。

- `## YYYY-MM-DD` 見出しの下に、テーマごとの `### ` 小見出しで追記する
- 日本語。既存のスタイル（太字のリード＋箇条書き）に合わせる
- **何をしたか**ではなく**なぜそうしたか**を書く。検討して却下した案も残す
- 計測値があれば必ず数字で入れる（変更前後のサイズ、リクエスト数、fps など）
- 踏んだ罠と、その原因が何だったかも書く。同じところで詰まらないため

## テスト方針

Vitest（node 環境）で、**React も three.js も含まない純粋ロジックだけ**をテストする。DOM もレンダラも要らないので全体が高速に回る。対象はカメラ座標計算・球面の幾何・星屑シミュレーション・決定論的ハッシュ・実績フィルタ・言語解決・URL 解釈など。

新しいテストは**変異テストで検証してから完了とする**。わざとバグを仕込んで落ちることを確認していないテストは、緑でも意味がない。

- **変異を戻すのに `git checkout <file>` を使わない。** 未コミットの変更ごと消える（実際に消した）。`cp` でスクラッチパッドへ退避してから当て、`cp` で戻す
- **定数を自分自身と比べるテストは何も言っていない。** `shouldReturnHome(ABOUT_RETURN_AT, ...)` はしきい値を 1 にしても緑のままだった。閾値まわりはリテラルで書く
- **テストが通す入力が現実の経路かを確かめる。** `glidePose` の弦バグを最初のテストは捕まえられなかった。両端の方位が同じ「スクロールで抜ける」経路で書いていたためで、方位も回る「HOME ボタン」の経路にして初めて落ちた
- **実装の式を言い換えた assert を書かない。仕様をそのまま書く。** 変異テストは「テストと実装の食い違い」は捕まえるが、**「テストと実装が揃って仕様から外れている」ことは原理的に捕まえられない。** セクション寄りの姿勢は `camera.up` が world +Y のままで**看板が鏡文字**になっていたのに、テストは `angleAt(10) ≈ π/2 - SECTION_TILT` と**実装の式をそのまま写して**緑だった。しかも `it("looks down at the subject rather than up at it")` は赤道上の建物で `forward` がちょうど world +Y になるため**縮退しているからこそ通っていた**。姿勢の類は「`camera.up` と視線が30°以上離れている」のように、**壊れたときに何が起きるかを直接書く**（`worldLayout.test.ts` の `describe("sectionUp")` が実例。詳細は [docs/review.md](docs/review.md) A-1/A-2）
- **定数の docstring が間違っていると、レビューする側が定数を信じて実装を疑う。** `SECTION_TILT` は「その建物のローカル地平線からの仰角」なのに「法線から傾ける角度」と書いてあり、そのせいで「cos/sin が入れ替わっている」という**誤診が1回まるごと生まれた**（入れ替えると仰角70.5°の真上見下ろしになり、参考写真から遠ざかる）。値ではなく説明のほうを直すべき場面がある
- **無言で失敗する機構は、機能したことを別途テストする。** near ズームでリーダーラインとスポットライトマーカーが完全に死んでいたのに（マーカーの投影 y がビューポート外に落ちて可視判定が恒常 false）、`hide()` は設計どおり静かに何もしないので誰も気づかなかった。**距離・オフセット・構図を触る変更は、それが殺しうる「見えるはずのもの」を名指しで検算する**（`scene/markerScreen.ts` の `markerOnScreen()` のように純関数へ切り出し、実測した最悪ケースを fixture でテストする）

## 3D シーンを触るときの前提

詳細は [docs/tech.md](docs/tech.md) にある。壊しやすい不変条件だけ挙げる。

**トップページは宇宙に浮かぶ球体の土台（惑星）で、カメラはその中心を見ながら一定の高度を回る衛星。** 平面の浮遊島からの移行は完了済み——経緯・却下案・実測値は [docs/planet-migration.md](docs/planet-migration.md) にまとまっているが、**通常の作業でこのブランチの3Dを触るのに読む必要はない**。現在の構造はここと `docs/tech.md` が正。

- **ツアー経路は緯度経度の空間で補間する。方向ベクトルを slerp / スプラインして正規化してはいけない。** 球面上の補間として「正しい」のはそちらだが、区間ごとに大円をたどることになり、**大円は両端より極側へ膨らむ**（南緯40度・経度差144°で29.8°）。実際にカメラが緯度-71°まで潜ったことがある
- **地形のノイズ・建物の向きは方向ベクトルの成分の関数にする。緯度経度で書かない。** ±180°の子午線に崖が、極に風車模様が出る（あらゆる経度が1点で出会うのに、ノイズが同じ点に別々の高さを返す）
- **建物・装飾は世界の +Y ではなく、自分の立つ点の法線を「自分の上」として立つ**（`tangentBasis()`）。カメラがある建物を画面に収める `sectionPose()` も同じ——建物ごとの接線基底（法線＝up、地表沿いの北＝forward）から位置を組むので、緯度に依存しない。世界の +Y を基準にした古い `framePose` 方式は、緯度の高い建物でカメラが地表の下に潜る欠陥があった
- **セクションへ寄っているあいだは `camera.up` もその建物の法線にする**（`sectionUp()`）。**姿勢は位置と注視点だけでは決まらない。** 構図をローカル座標系で組みながら `camera.up` だけ world +Y に残すと2つ同時に壊れる: (1) 赤道付近の建物のローカル「上」はほぼ水平なので**建物が画面上で横倒しに描かれる**（実測84°ずれ）、(2) カメラが立つローカル北も赤道付近ではほぼ world +Y なので**視線が `camera.up` から10〜18°しか離れず `lookAt` が縮退**し、ロールが不定になる（＝看板が鏡文字）。自由回転の `ORBIT_MIN_POLAR`/`ORBIT_MAX_POLAR` は**セクションには効かない**ので、ここは構造的に安全である必要がある。飛行中は `slerpDirection` で補間し、**`updateCameraUp()` は `setLookAt` の前**に呼ぶ（`camera-controls` は渡された位置を `camera.up` 基準の球面へ分解する）
- **惑星の中心を軸に回るもの（自由回転・About）は `orbitPose`/`orbitAnglesOf`、建物へ寄るものは `sectionPose`。** 両方とも `scene/worldLayout.ts` にあり、カードを避ける横寄せは `focalOffsetX()` に一本化されている。「セクションへ寄るときは焦点オフセットを明示的に0に戻す」のような二本立ての規則は増やさない
- **ドラッグを離したあとの復帰だけは「速度」で戻す（`ORBIT_RETURN_SPEED = AUTO_ORBIT_SPEED × 2`）。damp 単体に戻さない。** damp は残差の割合を詰めるので**距離に関わらず所要時間が一定**（λ=5 で約0.5秒）＝大きくドラッグするほど速く引き戻される。毎フレームの詰め幅は `orbitStepFraction()` の係数1つを方位・極角の両方に掛ける形（damp 2本と等価）で、そこに `min(damped, maxArc·Δt / arc)` の上限を被せてある。3つ壊しやすい: (1) **上限は大円の弧で測る**（`orbitArcBetween()`）——方位で書くと `sin(polar)` ぶん、極付近だけ速く走れる。(2) **等倍にはできない**——復帰中も `tourURef` は前進しており目標自体が `AUTO_ORBIT_SPEED` で逃げているので、等倍だと真後ろから追う最悪ケースで永久に着かない。(3) **上限を掛けるのは復帰だけ**。ホイールとカードのスワイプに掛けると、読み手が要求した移動が「入力を無視された」ようにしか見えない（0.4 rad のフリックに6秒）。差分の大きさでは復帰と区別できないので、`returningRef` という明示的なモードで持つ——立てるのは「離した瞬間」だけ、降ろすのは次の `pointerdown`／ホイール／`turnRequest`／飛行の開始と、上限が damp より緩くなった瞬間（＝到着。独立した閾値は持たない）
- **カメラの向きの真実は `viewRef`（azimuth/polar）、ツアー上の位置の真実は `tourRef`（`tourURef`）と、別々に自分で持つ。** `CameraControls.azimuthAngle` を読み戻さない。極のクランプも自前で持つ（`setLookAt` は `minPolarAngle`/`maxPolarAngle` を見ないため、`camera-controls` 側は `[0, π]` に開けておく）
- **1モデル＝1ドローコール。** 街は `InstancedMesh` で数千ブロックを描いている。共通格子で足りるものは `VoxelModel`、1個ずつ向きが違うものは姉妹コンポーネントの `PlacedVoxels` を使う。オブジェクトを増やすときもこの原則を崩さない
- **プラザ（観覧車・木・街灯・住人の共有地）のような「みんなが立つ場所」は、決め打たずに探す。** `scene/planet/decor.ts` の `pickClearing()` — 条件（地表が草・既存の建物と重ならない）を満たす候補が無いときの段階的なフォールバックまで含めて設計する。見つからなかったときの分岐を呼び出し側に持たせない
- **移動する経路が閉じた式（`offsetDirection` など）なら向きもその微分の閉じた式で求め、経路がブラックボックス（スプラインなど）なら `tangentOf()` で数値的に接平面へ射影する。** どちらも `scene/planet/planetLayout.ts` にある
- **太陽は半径 `SUN_DISTANCE`（41）の球面から出さない**（`scene/sunLight.ts`）。`shadow-camera-*` の4値はこの距離とシーンの外接半径だけから導かれていて**向きに依存しない**ので、球面上で回すかぎり再調整が要らない。外すと影のフラスタムがシーンを取りこぼし、**影が黙って描かれなくなる**（`sunLight.test.ts` が見張っている）
- **固定光では全エリアを照らせない。** 5エリアは球面上で149.5°に散らばっており、全球総当たりでも「最も暗いエリア」を0より上げられる固定方向は存在しない。**カメラ追従の主光源は実装して却下済み**——明るさは解決するが影が建物の裏に隠れて見えなくなり（砂地の影面積 26.9%→20.6%）、光がスクリーンに固定されるので惑星が回っても陰影が変わらない。**同じ案を再提案しない**。現在の答えは「読み手が太陽を掴んで動かす」（`scene/Sun.tsx`）
- **見た目の太陽を遠くに置かない**（`SUN_ORB_RADIUS = 34`、光の `SUN_DISTANCE = 41` とは別）。**カメラは常に惑星の中心を見ているので視野はその1方向まわりの約33°×22.5°の円錐しかなく**、遠方の物体がそこに入るには惑星の真裏にいるしかない——そこでは惑星に隠れる。半径260で試して**ツアー6点中0点**の可視だった。窓は実質 34〜40 しかない（俯瞰から見て惑星の円盤9.7°の外／画面の縦半分22.5°の内／地表に立つものの外）。大きさは画面pxで持つ（`markerScaleForScreenRadius`）
- **太陽のドラッグを「差分ぶん回す」で書かない。原理的に追従しない。** 半径34の球を外から見ると、太陽は円周の大部分で**視線に沿って**動くので画面上ではほとんど動かず（正対位置で運動の94.7%）、必要な補正係数はシルエットで発散する。実測で追従率41%。**ポインタから球へレイを飛ばして交点に置く**（`sunDirectionAlong()`）と係数が要らなくなり、実測100%
- **太陽が出るのも動かせるのも俯瞰モードのときだけ**（`OrbitZoom = "far"` かつセクション非選択）。標準では画面の角に切れて一周の1/3しか映らない。**ただし光そのものは止まらない**——置いた場所からどの高度でも照らし続ける
- **夜側の環境光の底（`ambientLight` 0.42 / `hemisphereLight` 0.52）を下げない。** 太陽を自由に置ける以上、これが無いと夜側は「輝度40未満＝形が読めない画素」が14.1%になる（底ありで0.2%）
- **陰影の強さを「画面全体の標準偏差」で測らない。** 地形と建物の固有色の変化が支配的で、陰影の差が埋もれる（実測 sd 35.0 vs 35.2、局所コントラスト 4.66 vs 4.65 で差が出なかった）。**単色の領域（砂地）を選んで測る**と 26.9% vs 20.6% と明確に出る
- **カメラの行き先を決める effect は `scene/Scene.tsx` に1つだけ。** 経路も7つだけ（セクションへ寄る／Aboutへ寄る／リセットでツアー先頭へ／Aboutを読み切った＝何も飛ばさない／セクションを離れてツアー再開／自由回転のズーム段階が変わった（ピンチ／ズームコントロール）／初回マウント）。ここを分散させない
- **自由回転には2つの固定高度（`OrbitZoom`＝`"near"`/`"far"`）があり、AppStateContextの`orbitZoom`が真実。** `near`（新デフォルト）と`far`（俯瞰、旧来の唯一の高度）を`worldLayout.ts`の`orbitRadiusForZoom()`で解決し、`Scene.tsx`内で`ORBIT_RADIUS`を直書きしていた箇所は全部これに置き換わっている。HOME/ロゴ/セクション離脱は**必ず`near`に戻る**（`goHome`/`closePage`/`openPage`/URL同期の`applyUrl`がそれぞれ`setOrbitZoom("near")`を呼ぶ）——`far`はピンチかズームコントロールで能動的に選んだときだけの一時的な状態
- **`near` の縦オフセット（`NEAR_VERTICAL_SHARE = 0.636`）は「カード↔マーカーの点線」を承知で捨てている。勝手に直さない。** 惑星を画面下へ押すと**カードが説明しているエリアごと画面の下へ出る**——正面のエリアは円盤の中心ではなく5アンカーの最近傍で、ツアーにつれて円盤全体を動き回るため。一周実測で 0.78 は20サンプル中16が画面外、全部収まる上限は 0.04＝実質ゼロ。惑星を半径185（About と同じ豆粒）まで引かない限り**両立しない**。`reference/image7.png` の構図を取る判断が下されており、`worldLayout.test.ts` の `it("is knowingly past the point where the facing marker stays in frame")` がそれをピン留めしている（**緑になったら仕様変更**）。実測表は [docs/review.md](docs/review.md) A-3
- **値は参考写真から導く。目測で置かない。** 惑星の中心は注視点そのものなので画面の `0.5 + share/2` に来る。`image7.png` の「中心が81.8%下」は share 0.636 と一意に決まるのに、目測で置かれた 0.86→0.78 は中心を89%まで押していた——**参考写真に合わせたつもりで0.15ぶん行き過ぎていた**
- **座標計算は `scene/worldLayout.ts`／`scene/planet/*.ts` に純粋関数として置き、three.js を介さずテストする。** `camera-controls` の `fitToBox` は内部でカメラ角度を90°刻みに丸めるため、斜め俯瞰のこのシーンでは使えない
- **自由回転へ降りる飛行は、着地で `azimuthRef`/`polarRef`/両ターゲットを揃えてから `glideRef` を落とす**（`scene/cameraFlight.ts` の `Flight.land` → `FlightFrame.landed`）。**カメラの transform と `azimuthRef` は別々の真実で、飛行は前者しか動かさない。** 揃えないと次のアイドルフレームが**飛行前の角度から** damp を再開し、60fps では1フレームで8%しか進まないので**旅の92%が1ティックで巻き戻る**（実測: セクション→ホーム最大36°、ロゴ→ホーム最大180°）。開いたタイミングで量が変わるので「たまにカクつく」に見える。セクション／About への飛行は `land: null`（前者は停める、後者は自分で毎フレーム書く）
- **`frameloop` を切り替えると R3F が `clock.elapsedTime` を0に戻す。** 詳細ページの開閉ごとに2回起きる。`sceneClock` が単調化を自前で持っているのはこのためで、外すとトラック・雲・住人・マーカーの脈動が**惑星が見えている状態で**一斉にワープする。`setPaused` も同じ補正を通す
- **飛行は自前の時計とイージングで回す。`setLookAt(..., true)` に戻さない。** `smoothTime` のダンピングは出だしが最速なので、詳細ページから戻る飛行はシートが退く0.3秒のあいだにほぼ終わってしまう。`lerp()` も使わない（±π をまたぐと長い方へ回る）。`glidePose()` と `easeInOutCubic()` を通す
- **飛行の初回フレームには `delta` を加算しない。** `delta` は前フレームからの経過で、飛行はフレームの**間**に始まる。`frameloop="demand"` の裏では毎秒1フレームなので、加算すると1フレームで飛行が終わる
- **自己紹介の帰りは飛行ではなくスクロールが握る。** 進捗 `ABOUT_RETURN_FROM`（現在 0.35）〜1 を `glidePose()` に通して毎フレーム構図を組む（`hub/about/aboutScroll.ts` の `aboutReturnProgress()`）。**この定数を下げてもカメラは速くならない**——旅は常に「列1本ぶんのスクロール」なので、下げるほど同じ旅が列の広い範囲に引き伸ばされ、読書のどの時点でも惑星が先に進んでいる状態になる。限界を決めるのもそこで、**惑星は文字がまだ画面にあるうちに育つ**（0.35 では 0.6/0.7/0.8 の実測で干渉しないことを確認済み）。**着地先は「今の方位・Aboutの極角のまま半径だけ寄せる」固定点ではなく、ツアー経路上をアイドル巡回と同じ速度で動き続ける点にする。** 固定点だとツアー曲線の緯度帯の外に着地し、着地後の`damp`が数十度を引き戻すワープになる（ツアーは緯度経度空間の曲線で、方位ごとに「正しい極角」が決まっているため）。`tourURef`をAboutが開いている間ずっと進め続け、`azimuthRef`/`polarRef`/両ターゲットも毎フレームその値に合わせておけば、`t=1`は「停止」ではなく「速度が一致した瞬間」になり、着いた後に飛行を足す必要も、着地後の再投影も要らない — `onFinish` は `closePage` だけを呼び、行き先の effect のハンドオフ分岐は `facing` の即時反映だけをして抜ける。
- **自己紹介の列はスターウォーズのオープニングロール風の3Dクロールで、見た目は `reference/image9.jpg` の実測に合わせてある。** 寸法・角度・サイズの根拠は [docs/tech.md](docs/tech.md) の「クロールの見た目は reference/image9.jpg の実測に合わせてある」に集約した。ここには壊しやすい不変条件だけ挙げる。
  - **`rotateX` は framer-motion が触らない要素に置く。** `x`/`opacity` の入場アニメが同じ `transform` プロパティを毎フレーム書くため、`perspective` を持つ非スクロール要素の内側で、スクロールする要素自身に `rotateX` と `transform-origin:50%_100%` を掛ける。
  - **手前の行が扇状に歪むのは `perspective` が小さすぎるとき。列の位置ではない。** 160px に対して箱の奥行きは763px＝スケール比5.8倍という超広角で、これが「字が伸びて読めない」の原因だった（`image9.jpg` の可読帯は3.3倍程度）。460px / `rotateX(110deg)` / `perspective-origin: 50% 15%` / `h-[350%]` まで緩めて解消。**一度「中央寄せが効いた」と誤って結論づけた**——中央寄せ後・`perspective` 160px のままのクロップ実測でまだ歪んでいたので、効いていたのは遠近のほうだった。列は惑星を避けて左に寄せてあるが、歪みは戻っていない。
  - **マス目は `w-[15em]` と `text-[min(4.8vw,4.5rem)]` の2つで1つの仕掛け。片方だけ動かすと壊れる。** `em` は `font-size` 以外では自分自身の算出サイズを指すので、両方を同じ要素に置くと幅＝全角15マスになる。**`4.8vw` は見た目の好みではなく天井で、マス数を変えるたびに計算し直す側の値**——スクローラは `w-[75%]`＝75vw なので上限は `75 ÷ 15 = 5.0vw`、`15 × 4.8vw = 72vw` はそこに4%の余裕を取ったもの。はみ出すと `overflow-y:auto` が `overflow-x` を格上げする罠に落ち、遠くの行まで一律に虫食いになる。**マス数そのものは構図の選択で、image9 の実測は13、サイトは指示により15**——参考写真から導いたのは「全行が同じ文字数・両端そろい・禁則なし」という組み方であって、13という数ではない。ブレークポイント4段（`text-3xl sm:text-4xl lg:text-6xl xl:text-7xl`）は**この方式に置き換わって廃止済み**。あれは同じ目的の近似で、実測すると 9.7／14.6／13.8／15.0 文字とばらついていた
  - **日本語コピーは全角文字だけで書き、段落は1ソース行に収める。** 半角英数字は約0.5マスなので混ざるとその行だけ格子から外れ、JSX はテキスト途中の改行を半角スペースに畳むので段落を2行に折ると**ファイルに無いスペース**が入る（1段落目が実際にこれで崩れた）。どちらも**表示は壊れないまま格子だけが静かに崩れる**類なので、`src/hub/about/aboutCopy.test.ts` が見張っている。`text-spacing-trim: space-all` / `text-autospace: no-autospace` を明示してあるのも同じ理由（ブラウザ既定値がまだ動いているプロパティで、1em未満の空きが勝手に入る）。禁則は image9 に合わせて切ってある（`line-break: anywhere`）——残すと句読点が送られた行だけ12文字になり、両端揃えがその1マスを字間に配る
  - **左右のはみ出しは箱ごとビューポートより広げて出す。** `overflow-y: auto` は CSS 仕様上 `overflow-x` を `auto` に格上げするので、スクローラの内側だけを広げるとローカル空間で切られ、遠くの行まで均一に虫食いになる。
  - **自動再生の速さは px/秒ではなく行/秒で持つ**（`ABOUT_AUTO_SCROLL_LINES_PER_SECOND`）。px レートは暗黙に文字サイズを埋め込むため、本文を拡大しただけで115秒の読み物が8分になり、ブレークポイント4段で4通りの速さになる。行高は本文ブロックから `getComputedStyle` で読む（スクローラは `line-height: normal` を返す）。**`NaN` を `scrollTop` に入れると 0 に落ちてクロールが先頭で固まる**ので、ガードは必須。**行高・`proseBottom`・スクロール上限は毎フレーム読まずにキャッシュする**（`metricsRef` + `ResizeObserver`）——読み→`scrollTop`書き→読みは毎フレーム2回レイアウトをフラッシュし、それが帰りのカメラと完全に時間で重なる。
  - **ホイールは `scrollTop` を直接動かさず、目標だけを動かす**（`scrollTargetRef` → `scrollCatchUp`、λ=12）。**カメラの構図が `scrollTop` の直接の関数**なので、即時加算するとトラックパッドのフリックの delta 粒度がそのままカメラに出る（自動再生中だけ滑らかで、手で加速するとカクつく、という症状になる）。目標は自前の数なので `scrollTop` のような自動クランプが無く、両端で clamp しないと overscroll が貯まる。**寄せは実時間、自動再生はシーンの時計**——ポーズ中もホイールは届くべき（`Scene.tsx` の飛行が実 delta を使うのと同じ理由）。カメラ側で damp する案は、文字が跳んでカメラが遅れて追う二重の動きになるので採らない
  - **列は `requestAnimationFrame` で自動的にも進む**（ホイールは加速・逆走の手段であって必須ではない）。About はキャンバス外の DOM なので `useFrame` は使えないが、時刻は自前の rAF ループから `sceneClock.delta()` を経由して読み、停止ボタンで他の自走物と同じフレームで止まる。
  - **体感速度はヘッドレスで測れない。** rAF の発火間隔が SwiftShader の描画負荷でページ全体1fps程度まで落ちる。純粋関数テストと「停止/再開でスクロール位置が変わらない/また動き出す」ことだけを確認し、ペース感は実機判断に委ねる。
- **カードとカメラを相互に更新しない。** 正面のエリアは `facingSectionOnPlanet()` が方向ベクトルから決め、カードはそれを読むだけ
- **カードの送りは「変更」ではなく「要求」。次を受け付ける前に、必ず到着（`section` が実際に変わったこと）を待つ**（`AreaCard.tsx` の `pendingSince`）。待たないと `Hub.tsx` の `adjacentOnTour(card, step)` に**退場中のカード**を渡すことになり、同じセクションを指してスクロールが空振りする——`facing` はカメラが damp で振れてから返ってくる値（λ=5 で約0.13秒）なので、送った直後の `card` はまだ古い。**ホイールのしきい値で代用しようとしない**（速さの問題ではなく、順序の問題）
- **カードのホイールのジェスチャ区切りを「静止」だけで判定しない**（`hub/cardWheel.ts`）。トラックパッドの慣性テールは1〜2秒 wheel を吐き続けるので、テールに重なった2回目のフリックが丸ごと飲まれる（＝「スクロールが効いたり効かなかったりする」の正体）。テールが単調減衰することを使い、**山の半分を下回ってから2.5倍以上に跳ね上がった**イベントも新しいジェスチャとして扱う。`REARM_DECAY_SHARE` と `REARM_RISE` は**2つで1つの仕掛け**で、減衰の条件を外すとフリック自身の立ち上がりを2回目と誤認する。`deltaY` は `wheelPixels()` で `deltaMode` を吸収してから使う（行モードだと1ノッチ=3で、しきい値80に27ノッチ要る）
- **頻繁に変わる値を `<Canvas>` より上のコンテキストに置かない。購読チャンネルで運ぶ**（`scene/markerScreen.ts`／`hub/about/aboutScroll.ts` の `aboutReturn`／`state/facingChannel.ts` の3つ）。R3F 9 の `CanvasImpl` の setup effect には依存配列が無く、再レンダーのたびに `configure()` と `root.render()` がシーン全体を再調停する。**`React.memo` では止められない**——`its-fine` の context bridge が `CanvasImpl` のレンダー中に上位の全コンテキストを購読するので props に関係なく再レンダーするうえ、memo すると今度は `CameraController` が `activeSection` を受け取れなくなる（Bridge は `root.render` 経由でしか値を運ばない）。`facing` はこれで Context から出した
- **`scene/planet/` の基底を three.js の姿勢に変えるのは `scene/planetPlacement.ts` だけ。** 毎フレーム動くものは `orientTo`/`standOn`（scratch へ書く）、一度きりの配置は `quaternionOf`（タプルを返す）。`new Matrix4()` を `useFrame` の中に書かない
- **線の形は `hub/lightningPath.ts` が持つ（規則的なジグザグではなく稲妻の折れ線）。端の連続性は包絡線で担保し、端をピン留めする特別扱いを足さない。** `lift` に `min(along - from, to - along)/RAMP` を掛けてあるので、頂点は必ず軸上（lift 0）で生まれて育つ——旧「余りの山が余白に応じて育つ」分岐はこれに置き換わって削除済み。**折れ方を「平均回帰する歩行」にしない**（凸結合は中央へ引っ張るので、ゆるく波打つ線になる。一度そう実装して失敗した）。**引き直しの拍は `strikeIndex()` から取り、独立した周期定数を作らない**——`boltFlicker` と `FLICKER_PERIOD` を共有しているので「マーカーが光る瞬間に線も引き直される」が定義上ずれない
- **点線の起点（アンカーの点）を、作り直される要素の中に置かない。** カードは `AnimatePresence` で作り直されるので、中に置くと退場する側の ref コールバックが `null` で後から呼ばれ、`anchorRef.current` が空になって**線が最後の位置で凍る**。スタックのコンテナ側に置く
- **点線がずれて見えたら、まず「射影がずれている」のか「描き直されていない」のかを分ける。** 同じフレームについて publish 値・点線の終端・ドットの画素を突き合わせれば一目で分かる（回転中と停止中で終端が同じなら、それは凍っている）。ドットを画素の色で探す方法は、星屑や停止中にオレンジになるボタンを拾うので**オラクルにしない**
- **マーカーの大きさは画面 px で決め、ワールド scale を毎フレーム逆算する（`markerScaleForScreenRadius()`）。** 逆はできない。スプライトはビュー空間で頂点をずらすビルボードなので、ワールド空間のベクトルを射影しても画面サイズにはならない
- **マーカーは円ではない（水色の稲妻）。点線の切り詰めは必ず `markerClearance()` を通し、`halfHeight` をそのまま引かない。** インクの縁までの距離が方向ごとに違うため（真上0.61・真横0.45 × halfHeight）、引くと隙間が方向によって呼吸する。内接楕円は**先端がはみ出すので安全ですらなく**、凸包の支持関数は安全だが真下で1.67倍と緩い——どちらも一度検討して捨ててある。形・色・火花は `scene/markerBolt.ts` に集約（純粋モジュール）。輪郭の縦が ±1 に届くことが sizing の前提で、canvas に描くときは `lineJoin = "round"`（既定の miter は鋭角で `MARKER_GLYPH_FILL` の外へ棘を飛ばす）。**火花はサイズではなく明るさに掛ける**——サイズは点線が追いかけている数なので、掛けると先端が痙攣する。詳細は [docs/tech.md](docs/tech.md) の「エリアのマーカーと点線」
- **`useFrame` の中でカメラの行列を読む前に `camera.updateMatrixWorld()` を呼ぶ。** `camera-controls` は毎フレーム `position` / `quaternion` しか書かず、行列を張り直すのは `gl.render()` の中。これを飛ばすと `.project()` が前フレームのカメラで射影し、DOM 側だけ1フレーム遅れる（回転方向で符号が反転するズレになる）
- **自分から動くものは `scene/sceneClock.ts` から時刻を読む。** `state.clock.elapsedTime` や生の `delta` を直接使うと、停止ボタンで止まらないオブジェクトが1つだけ残る——drei の `<Stars>` はまさにこれで（ソース側が `sceneClock` を知らない）、`speed={0}` で止めて回避している
- **詳細ページがキャンバスを覆っている間は `frameloop="demand"`。** 省電力のためのこの切り替えを外さない
- **canvas は透明のまま（`<color attach="background">` を足さない）。** 惑星・建物が自己紹介の文字より手前に来るのは、canvas が透明で、その下の DOM 層（自己紹介の列 → 背景色 div）を不透明画素だけが隠す仕組みで実現している——z-indexではなく合成の透明度で解決しているので、背景色を canvas 側に戻すと文字が常に見えなくなる。色は `Hub.tsx` の背景色 div 一箇所だけが持つ。**canvas 自身の `pointer-events` は `<Canvas>` の `style` prop で操作する。** `eventSource` を渡さない限り R3F 自身が最も外側の div にインラインで `pointerEvents:'auto'` を書くため、祖先の CSS で `pointer-events:none` を継承させようとしても毎回打ち消される——`Scene` の `interactive` prop（`style={{pointerEvents:"none"}}` を直接渡す）が唯一効く場所。詳細は [docs/tech.md](docs/tech.md) の「canvas を透明にして、自己紹介の上に3Dオブジェクトを重ねる」

### 見た目を確認する方法

`curl` や一発の `chrome --screenshot` では**白いキャンバスしか撮れない**（WebGL の描画前に走るため。`--virtual-time-budget` は R3F の rAF ループのせいで永久にハングする）。スクラッチパッドに `puppeteer-core` を入れ、以下で撮る。

- `executablePath` に `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`、`headless: "new"`
- 起動引数 `--no-sandbox --enable-unsafe-swiftshader --use-gl=angle --use-angle=swiftshader`（ヘッドレスに GPU がないのでソフトウェア WebGL）
- `waitUntil: "networkidle2"` の後、描画されるまで **7秒ほど待ってから** `page.screenshot`
- `console` / `pageerror` を拾ってランタイムエラーを検出する
- 開発サーバーのスクショ左下に出る黒丸は Next.js の dev インジケーターであって、アプリの UI ではない

**ヘッドレスで「滑らかさ」は測れない。** SwiftShader はソフトウェアラスタライズなので、このシーンは**毎秒3フレーム程度**しか出ない（実測: 3秒間で4フレーム、間隔 333〜1233ms）。rAF の間隔を取ってもフレーム落ちの診断にはならず、時間で進むアニメーションは実時間で何倍にも伸びる。見た目の確認と状態の確認には使えるので、**動きの質はコード側の算術とテストで固め、体感は実機に回す。**

**カメラの飛行が絡む操作は、単純な「秒数×倍率」よりさらに待つ。** `MAX_FLIGHT_STEP`（`Scene.tsx`）が1フレームあたりの進行を0.1秒に固定でクランプするため、1.1秒の飛行は実時間の長さに関わらず最低11フレームを要る——毎秒3フレーム、間隔が1秒を超えるフレームもある実測を踏まえると、8秒待っても着地しきっていないことがある（着地前に撮ると、距離が変わる操作なのに「ほとんど動いていないように見える」という誤った結論を導く）。1回の待機時間を勘で決めず、**数秒おきに複数枚撮って値が収束したかを見る**のが確実。

**`page.mouse.wheel` は使わない。** CDP 経由で1回あたり3〜5秒かかり、実用にならない（About の長いスクロールで実測）。`window.dispatchEvent(new WheelEvent("wheel", { deltaY }))` を、対象の要素上で（`document.elementFromPoint(x, y)` から）まとめて投げるほうが速く確実——1回の `page.evaluate` の中で数十発ディスパッチしても即座に終わる。ホイールで進む量は `WHEEL_SENSITIVITY` から逆算できる。

**マウスドラッグも間隔を詰めない。** `steps` を大きくして待機を200〜300msまで詰めると、Chrome のタブが `Target closed` で本当に落ちることがある（惑星＋雑居ビル＋装飾一式でシーンが重くなってから顕著）。`steps=5` 程度・待機800ms以上に緩め、スクリプトが途中で落ちたらゾンビ化した Chrome プロセスを `pkill` してから次を走らせる。動きそのものを検証したいのでなければ、間隔を詰める理由はそもそも無い。

マーカーの画面座標などをヘッドレスで測って使うときは、**測定とクリックを同じスクリプト実行の中で行う。** アイドル回転がページ読み込みからの経過時間ぶん常に進んでいるため、別のスクリプト実行で測った座標を使い回すと、測った瞬間と実際にクリックする瞬間のずれでマーカーが動いてしまう。

構図をピクセルで測るときは、次の2つを守る。どちらも守らないと**存在しない差を測って、間違った値に合わせ込む**ことになる。

- **星屑や紙吹雪のような漂うオブジェクトを除外する。** 「背景色と違う画素の min/max」で島や惑星の幅を測ると、画面の隅まで漂うそれらが端を決めてしまう（旧・平面の島での実測でフレーム幅83%と出たが、本当の島は57%だった）。列ごとに画素数を数え、**フレーム高さの1割**を超える列だけを採用する。2% では縦の重なりが通ってしまう
- **一周ぶんサンプリングして最悪角度で判断する。** 回るとシルエットが伸縮する形をしているため、少ない角度数では見落とす（旧・平面の島で、5角度では右端を94.6%と報告した設定が、16角度に増やすと98%台の角度を複数出した）

## ボクセルアセットの制作

Blockbench デスクトップアプリを MCP（`localhost:3001/bb-mcp`）経由で直接操作して作る。アプリが起動していないとツールは動かないので、`mcp__blockbench__*` が見つからないときは起動を確認してもらうこと。

- キューブは**すべて XYZ = 1,1,1**。1モデル内でサイズを混在させない
- ネイティブのブロック数は**1辺 10〜80 程度に収める**。実寸に比例させない（部屋でも 500 角にはしない）
- 実寸への到達は R3F 側の `scale` / `voxelSize` 倍率に任せる。見た目の統一感は、カテゴリごとに倍率の段を決めて一貫させることで担保する
- **GLB 書き出しに `export_model` は使えない**（gltf コーデックで空の `{}` を返すプラグインのバグ）。`risky_eval` から `Codecs.gltf.compile()` を await して `fs` で書く
- `create_cylinder` の `texture` 引数は効かない。生成後に `risky_eval` で各 face に texture の uuid を入れ直す

経緯と判断理由は [docs/voxel-object-creation.md](docs/voxel-object-creation.md) にまとまっている。

## 設定とスキルの置き場所

Claude Code 用の設定はすべて `.claude/` に集約している（`.agents/` と `AGENTS.md` は廃止済み）。

- `.claude/skills/hallmark/` — デザインスキル。新規ページ制作・リデザイン・UI 監査のときに `/hallmark` で呼ぶ
